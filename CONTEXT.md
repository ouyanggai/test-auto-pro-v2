# 项目上下文

本文件只维护稳定术语和边界。产品行为见 `docs/PRODUCT.md`，技术方案见 `docs/ARCHITECTURE.md`。

## 术语

| 术语 | 含义 |
| --- | --- |
| 流程自动化测试平台 | 本项目唯一产品名称。 |
| 目标平台 | `参考代码/` 中的现有业务系统；其真实代码和运行结果是流程行为依据。 |
| V1 | 旧版测试工具，只用于参考经过使用验证的便利交互。 |
| 测试计划 | 保存流程来源、路径、动作、数据和运行方式的用户配置。 |
| 执行路径 | 单个真实流程实例预期经过的线路、节点动作和成功断言。 |
| 运行记录 | 一次启动后形成且不可覆盖的执行事实。 |
| 功能切片 | 一个独立计划、实施、自动验证并等待人工验收的最小交付单元。 |
| 参考仓库 | `参考代码/` 下由安全同步脚本维护、被当前 Git 仓库忽略的源码仓库。 |

## 边界

- 本项目拥有测试计划、运行编排、运行记录和工具侧设置。
- 本项目读取并操作目标平台，但不修改目标平台源码、权限或业务规则。
- 产品规则来自 `docs/PRODUCT.md`；目标平台细节必须回到参考仓库核实。
- 旧项目和 `重构指南/` 是历史资料，不是现行实现规范。

## 当前未决事项

- 目标平台连接和登录值只允许运行时通过 Go 后端环境变量或被 Git 忽略的本机配置提供，不进入仓库；当前工作区已沿用 V1 安全准备本机配置。
- F-004 已由用户验收通过；新发起读取模板树，已发和待发读取实例代理树，并使用流程专用树布局显示真实结构。
- F-005 已由用户验收通过；执行路径只保存真实分支选择，不提前保存历史业务数据、处理人、节点动作或运行事实。
- F-006 已由用户明确要求继续下一任务而验收；后端保留只读要求分析，前端不提供独立核对页面。
- F-012 已获用户批准并进入实施：保留 F-005 路径、F-007 人员和目标 `form-runtime` 基础，数据按目标接口原字段选择历史实例并深复制原始表单数据；同时覆盖 FormMaking 和目标平台 `vue_custom`/NoFormFlow 自定义表单，是否存在表单不改变数据链路，仅对当前路径条件字段做可证明的最小补丁。删除智能生成、旧规则目录、ConstraintIR、旧路径准备和旧动作循环；不新增自定义页面状态模型、字段映射器或渲染适配层，直接把数据交给复制的 `form-runtime`。动作目录按目标后端与 `GroupApproveManage` 的真实门禁重建，保存草稿与审批暂存分开，并把有序动作编译为同一主实例的恢复场景。F-012 只完成配置准备，不创建运行记录或调用目标写接口；实施任务为 `/root/f012_implementation`，当前状态为 `implementing`。
