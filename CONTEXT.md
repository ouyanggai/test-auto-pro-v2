# 项目上下文

本文件只维护稳定术语和边界。产品行为见 `docs/PRODUCT.md`，技术方案见 `docs/ARCHITECTURE.md`。

## 术语

| 术语 | 含义 |
| --- | --- |
| 流程自动化测试平台 | 本项目唯一产品名称。 |
| 目标平台 | `参考代码/` 中的现有业务系统；其真实代码和运行结果是流程行为依据。 |
| V1 | 旧版测试工具，只用于参考经过使用验证的便利交互。 |
| 测试计划 | 保存流程来源、路径、动作、数据和运行方式的用户配置。 |
| 执行路径 | 单个真实流程实例预期经过的线路、节点动作和成功断言。 |
| 运行记录 | 一次启动后形成且不可覆盖的执行事实。 |
| 功能切片 | 一个独立计划、实施、自动验证并等待人工验收的最小交付单元。 |
| 参考仓库 | `参考代码/` 下由安全同步脚本维护、被当前 Git 仓库忽略的源码仓库。 |

## 边界

- 本项目拥有测试计划、运行编排、运行记录和工具侧设置。
- 本项目读取并操作目标平台，但不修改目标平台源码、权限或业务规则。
- 产品规则来自 `docs/PRODUCT.md`；目标平台细节必须回到参考仓库核实。
- 旧项目和 `重构指南/` 是历史资料，不是现行实现规范。

## 当前未决事项

- F-016 执行器最小真实闭环已停在 `ready_for_manual`：首次真实写实测提交被目标受理（实例
  caf2046d896f477c81c819153fc7d52f，trace 5ad3ab3454cce08d），核验重读未见可见性，按矩阵判不确定停在待对账。
  该「实例可见性」问题已在 F-018 收口时定位并修复：根因是按实例精确复查时带了公司业务关联过滤，
  把本工具发起的实例整条排除（语义清单第 19 条）；修复后同一条路径的提交写实测判为确定成功
  （运行 13，实例 7bcf0c29f1054ba0bed5cf367ba2f2d2，trace 0715f0cdf7cc2700）。
  仍存的已知边界：场景中的导航步骤与对已有实例动手的恢复执行属 F-019；同意动作未在真实目标执行、未登记
  （该账号手上没有审批待办）；用户要求的 claude 整切片评审连续三次被网关拦截而跳过，待恢复后补做。

- F-018 对账与安全重试已在 2026-09-05 经用户授权做过一次真实不确定写并当场收口：恢复层原先整段不可达
  （四处缺陷）已修复并有真实运行证据（运行 12 首次真实对账、运行 13 验证修复）；
  「未生效 → 重放」的真实目标证据结构性不可达（发起类写拿不到该结论、恢复执行属 F-019、账号无审批待办），
  链路正确性由真实 MySQL 集成用例保证。已知边界：对账现场只在服务进程内存里，重启即丢失。

- 目标平台连接和登录值只允许运行时通过 Go 后端环境变量或被 Git 忽略的本机配置提供，不进入仓库；当前工作区已沿用 V1 安全准备本机配置。
- F-004 已由用户验收通过；新发起读取模板树，已发和待发读取实例代理树，并使用流程专用树布局显示真实结构。
- F-005 已由用户验收通过；执行路径只保存真实分支选择，不提前保存历史业务数据、处理人、节点动作或运行事实。
- F-006 已由用户明确要求继续下一任务而验收；后端保留只读要求分析，前端不提供独立核对页面。
- F-012 已完成 T01 至 T08 自动验证并进入 `ready_for_manual`：保留 F-005 路径、F-007 人员和目标 `form-runtime` 基础，数据按目标接口原字段选择历史实例并深复制原始表单数据；同时覆盖 FormMaking 和目标平台 `vue_custom`/NoFormFlow 自定义表单，是否存在表单不改变数据链路，仅对当前路径条件字段做可证明的最小补丁。删除智能生成、旧规则目录、ConstraintIR、旧路径准备和旧动作循环；不新增自定义页面状态模型、字段映射器或渲染适配层，直接把数据交给复制的 `form-runtime`。动作目录按目标后端与 `GroupApproveManage` 的真实门禁重建，保存草稿与审批暂存分开，并把有序动作编译为同一主实例的恢复场景。F-012 只完成配置准备，不创建运行记录或调用目标写接口；实施任务为 `/root/f012_implementation`，等待用户人工验收。
- F-012 复核修复已完成：服务可启动的迁移执行器、条件与落盘两级数字保真（载荷列 `LONGTEXT` + `json.Number`）、接入产品的真实动作门禁目录与实例动作容器、只读编译场景预览与拖拽排序、真实执行的集成门禁（17 个 MySQL 用例，0 跳过）。目标写调用为零已由静态端点清点和动态录制双向断言，禁用清单从动作目录实时导出。
