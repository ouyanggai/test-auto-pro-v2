# 测试策略

## 风险分层

- 单元测试：快速验证纯逻辑，分别放在 `test/unit/backend/` 和 `test/unit/frontend/`。
- 契约测试：验证稳定 HTTP 响应和接口边界，放在 `test/contracts/`。
- 集成测试：验证前台开发命令、端口、热重载、代理和多组件协作，放在 `test/integration/`。
- 手工验收：记录需要用户在浏览器或真实目标平台核对的步骤，放在 `test/manual/`。
- 固定样本：非敏感测试输入放在 `test/fixtures/`。

## 执行原则

- 所有测试用例和测试脚本必须保存在根目录 `test/` 并分类。
- 日常只运行当前功能相关测试，不运行无关全量测试。
- 自动验证必须实际执行；视觉与真实目标交互由用户手工验证，不自动启动浏览器。
- 测试不得依赖或写入真实凭证，不修改目标平台数据。

## F-000 验证范围

- Go build 与健康接口精确响应。
- 前端类型检查、生产构建和 `pnpm dev:f` 开发服务器 smoke。
- `pnpm dev:b` 的 Air 配置、健康接口和 Go 源码变更后的热重载。
- 两个前台命令均以前台日志和 `Ctrl+C` 停止，不建立 PID 或后台进程管理。
- 应用壳主题默认值、切换与 `localStorage` 持久化源码契约，以及 `NLayoutHeader bordered`/`NLayoutSider bordered` 的 1px 分隔线、无嵌入色的统一表面、至少 `32px × 32px` 且收缩后完整可点的 Naive UI 圆形触发器和主内容滚动边界。
- 13 个参考仓库的远端、分支、HEAD 与清洁状态。
- 参考仓库脏工作树保护必须拒绝同步。

## F-001 验证范围

- 计划名称、状态筛选及清空对本地 mock 数据的纯函数行为。
- 四种计划状态到唯一主要操作的映射。
- 未验证账号仅允许新发起来源；账号编辑后的验证失效、来源回退和选择清理契约。
- 流程模板、已发流程、待发流程三类本地候选字段语义、搜索过滤和固定批次追加边界。
- `/plans`、`/plans/new` 路由、Naive UI 表格、两列上置标签栅格、三类独立虚拟列表与 `FormRules` 动态条件规则。
- 字段就地错误、单次短暂消息、无页面级错误汇总、定时启动和隐藏字段旧校验清理。
- F-001 源码不包含后端、目标平台请求或 SID 生成逻辑。
- 流程选择外壳高度稳定、稳定吸顶返回入口、语义分隔线、减少动态效果和仅在目标未完整可见时的最小距离引导。
- 静态提示边界和内容区水平滚动配置。
- 前端类型检查与生产构建；视觉、主题和 1366×768 滚动效果由用户手工核对，不自动启动浏览器。

## F-002 验证范围

- 后端单元测试：`TARGET_*` 必填与非敏感默认值、本机文件加载、进程环境优先级、缺失/非法文件、测试路径隔离、敏感值不进入错误、密码加密边界、会话 TTL、账号隔离、同账号并发去重、不同账号不互阻、一次重登上限。
- API 契约测试：账号验证、模板列表、实例列表的参数、分页、成功 DTO、已发 `statusName`、模板备注与表单关联计数、稳定错误码，以及所有公开 JSON 不含 SID、密码、AES key、customerCode 或 platformCode。
- 假目标集成测试：真实登录请求结构、SID body/query/header 三处传递、模板读取只调用已批准模板接口、已发八种状态到中文 `statusName` 的映射、模板/已发/待发请求与字段映射、空列表、HTTP 与业务会话失效、一次重登、登录拒绝、坏响应、坏分页、目标 5xx、超时和 context 取消。
- 前端单元测试：250ms 搜索防抖、取消、account/source/query/version 迟到保护、分页追加去重、失败页重试、会话最终失效后的验证清理、三类 DTO 映射，以及模板 `groupName` 标签、空分组、编码不展示与不参与本地搜索、已发全部中文状态与未知值中文兜底、备注空值/长值、辅助字段和无主状态展示。
- 前端结构测试：生产新建页不导入候选 mock，继续使用吸顶返回入口、分隔线和独立内容滚动；断言 `96px` 虚拟行、`480px` 视口、`574px` 稳定外壳、备注两行省略、减少动态效果及过期技术文案已移除。
- 本机配置集成测试：使用临时 V1 YAML 和动态测试值运行同步命令，验证键映射、自动加载、`0600` 权限和完整性检查；不得读取开发者真实 `.env.local`。
- `test/run-f002.sh` 只聚合上述当前范围测试、Go build、`vue-tsc` 与 Vite build；不连接真实目标平台，不启动浏览器。
- 真实账号身份、三类真实数据和浏览器视觉由用户按 `test/manual/F-002.md` 现场验收。

## F-003 验证范围

- 后端单元测试：计划数据库配置、库名约束、计划字段与枚举校验、串并约束、未来定时、幂等键和仓储错误映射。
- API 契约测试：创建、列表、详情的成功和稳定错误；公开响应不得包含 `create_key`、SID 或凭证。
- 本机配置集成测试：从临时 V1 `runnerDb` 样本安全同步 `PLAN_DB_*`，保留 `TARGET_*`，验证 `0600` 权限并拒绝复用 V1 原数据库。
- MySQL 集成测试：先确认当前独立库只含 `schema_migrations` 和 `test_plans`；再创建经过严格前缀校验的随机临时库，实际验证迁移、CRUD、筛选、幂等、重复迁移和重连可读，结束只删除本次临时库。
- 前端单元与结构测试：真实创建请求、同次重试复用幂等键、loading 防重复、失败保留、成功导航、真实列表筛选与错误恢复、详情无路径状态，以及生产代码不导入计划 mock、不引入 Vue Flow/dagre。
- `test/run-f003.sh` 只聚合上述 F-003 测试、Go build、`vue-tsc` 和 Vite build；不启动浏览器。
- 计划创建后的页面、视觉、筛选与前后端重启持久化由用户按 `test/manual/F-003.md` 人工验收。

## F-004 验证范围

- 后端单元测试：计划持久化身份透传、节点类型归一、直线、条件、手动分支、并行、嵌套、共享后继 `mergeTargetId`、无主线后继时省略汇合提示、非显式跨分支共享节点拒绝、自连接拒绝、未知普通节点、循环、关键 ID、空分支以及 500 节点和 200 深度边界。
- API 契约测试：流程图成功 DTO、路由汇合提示及普通节点省略规则、计划不存在、目标不可见、空结构、结构异常、登录失败、会话失效、配置缺失、不可用和超时；公开响应不得包含 SID、`flowProxyId`、目标原文、审批配置或字段权限。
- 假目标集成测试：新发起通过顶层 `ids` 精确核对模板、拒绝 `data.id` 伪筛选，并验证混入其他模板时仍只接受保存 ID；已发和待发先按实例 ID 精确取得当前 `flowProxyId`，再用代理 ID 读取实例树；会话失效时整条核对与详情链只重放一次。
- 前端单元测试：流程专用树布局确定性、主干垂直、分叉/汇合共享水平轨、两级路由严格从左到右、不同宽度和深度分支、嵌套路由不重叠、内层无独立汇合时继承祖先停止点并由外层统一汇合、透明路由节点、方向动画配置、根业务节点可读初始视口、页面全屏宽度变化的水平观察中心补偿与快速往返恢复；结构测试确认按钮和 `Esc` 共享串行状态机，以及重复、循环和断连 API 图经安全布局结果返回统一错误而不抛出、请求取消。
- 前端结构测试：Vue Flow 与 Controls 依赖和样式完整且不再依赖 dagre；页面内固定全屏、`Esc` 退出和卸载清理不使用浏览器 Fullscreen API；自定义正交边在减少动态效果时停止动画；异常布局在稳定画布区域保留计划摘要并绑定重试；画布允许平移/缩放但禁止拖动节点、连接、选择和删除，流程图展示组件自身不直接访问路径持久化 API。
- `test/run-f004.sh` 只聚合上述 F-004 测试、Go build、`vue-tsc` 和 Vite build；不启动浏览器、不连接长期真实目标数据。
- 三类真实流程、复杂分支、画布操作和深浅主题由用户按 `test/manual/F-004.md` 人工验收。

## F-005 验证范围

- 后端单元测试：从新发起根节点或已有实例活动入口解析路径；条件/手动单选、并行全包含、嵌套并行中的多处分支选择、汇合去重、直线零选择、缺失/重复/额外选择和非当前图 ID 拒绝；新发起多路径与同线路复制、已发/待发单路径限制；已成功幂等重试在目标图再次读取前返回原路径且不跨计划泄露。
- API 契约测试：路径列表、幂等创建、替换选择、删除、计划/路径归属、计划锁定、真实图变化后的稳定冲突，以及响应只包含选择所需的不透明节点/分支标识，不包含幂等键、SID、凭证或目标接口原始响应。
- MySQL 集成测试：在严格前缀随机临时库实际执行新增迁移，验证路径与选择事务、稳定序号、相同创建键重试、计划真实 `pathCount`、级联删除和重连读取；额外覆盖创建路径 1/2、删除最高序号 2、再次创建得到 3，确认内部计数器不会回退；不删除或修改现有开发库数据。
- 假目标集成测试：三类来源重新核对、已发 `currentNodeProxyId`/活动节点入口、待发 `flowNodeProxyId` 入口、待发页数存在或省略时的 20 页硬上限、会话失效整链只重放一次，以及写入前目标结构改变时拒绝旧选择；不调用目标写接口。
- 前端单元测试：可达路径解析、空入口拒绝、条件/手动选择替换、并行全选且不可取消、嵌套待选择数量、草稿协调与刷新失败保留、`selected/candidate/dimmed` 分类、实时摘要投影、确定性下一待选点、扣除面板的安全视口、路径列表加载门禁、路径复制和已发/待发新增限制。
- 前端结构测试：普通页面禁用选择、线路选择自动进入页面全屏、右侧可收起摘要面板、分支标签点击区、仅已选线路动画、弱化标签可操作、`aria-live` 气泡、失效保存恢复、删除确认、减少动态效果、画布节点仍不可拖动或连线；生产代码不出现表单数据、人员、动作或运行接口。
- Go 注释结构测试：F-005 新增或修改的具名函数和方法必须有紧邻声明的中文职责/边界注释，导出函数注释以符号名开头；核心业务、事务、幂等和异常兜底另有紧邻实现说明。
- `test/run-f005.sh` 只聚合上述 F-005 测试、Go build、`vue-tsc` 和 Vite build；不跑无关全量测试，不自动启动浏览器。
- 用户按 `test/manual/F-005.md` 使用现场新发起、已发、待发和含嵌套并行的复杂流程核对真实交互、刷新持久化与路径数量。

## F-006 验证范围

- 后端单元测试：只投影当前保存路径的主线与并行分组；已选条件的顺序、`and/or`、字段对常量、字段对字段、最后分支兜底和手动分支中文语义；未知判断、字段歧义和缺失配置降级为“需要人工核对”。
- 人员与动作单元测试：指定人员、角色、组织范围、发起人、主管、分管关系、职级、扩展属性、审批人自选和表单人员的四类状态映射；会签/竞签、人数、`isSkip`、提交、审批/协同结果以及空节点/结束节点无需动作。
- API 契约测试：计划与路径归属、无路径、当前图变化后的路径失效、三类来源成功响应、部分规则需核对及目标平台稳定错误；公开 JSON 不得包含 SID、目标节点/分支 ID、字段英文名、目标枚举代码、表单原始 JSON 或目标 envelope。
- 假目标集成测试：新发起模板详情与表单模板详情、已发/待发代理详情与表单代理详情、条件和审批配置解码、会话失效整条只读操作只重放一次、8 MiB 响应上限、超时和取消；不调用任何目标写接口。
- 后端分析与契约测试：路径条件、人员、动作、字段消歧、`contains` 真实方向、多活动入口分组、共同汇合去重、三类来源、路径失效和公开字段安全；不调用目标写接口。
- 前端结构测试：确认不再存在独立 `/plans/:id/requirements` 路由、计划配置页入口、RequirementsView、requirements.ts 和页面专用单测；F-005 画布右上角全屏及路径入口边界不得改变。
- `test/run-f006.sh` 只聚合上述 F-006 测试、F-005 路径遍历必要回归、Go build、`vue-tsc`、Vite build 和 `git diff --check`；不跑无关全量测试，不自动启动浏览器。
- 用户按 F-006 人工清单确认独立核对入口消失及 F-005 路径配置行为无回退；后端只读要求能力由聚合测试验证，后续配置页面另行规划。

## F-007 基础范围（已归档）

- 只保留 F-005 路径交互、真实人员投影、FormMaking iframe、运行时维护和目标只读请求边界的必要回归。
- 不再测试或维护智能生成、样本兜底、旧规则目录、旧动作循环或旧 P 类契约；`vue_custom`/NoFormFlow 自定义表单属于 F-012 的统一历史数据范围，保留其数据运行时与安全只读请求测试。

## F-012 验证范围

- 后端单元测试：精确 `flowCode + 规范数据身份` 候选过滤、全状态排序、FormMaking/自定义表单统一快照深复制、模板/页面版本差异、真实 JSON 路径映射、缺失/删除字段、`empty/needs_input/ready/affected` 状态派生。
- 分支单元测试：`lt/gt/lte/gte/eq/neq/contains/is_update/is_not_null/boolean_value`、BigDecimal 转换、B 包含 A、固定值/字段右值、顺序 `and/or`、首个命中、末分支兜底、手动分支、并行、最小补丁唯一性和不可满足阻断。
- 动作门禁单元测试：保存草稿与审批暂存分离、提交/重提、同意/不同意、回退前驱、加签当前人工待办与私有代理、移交演员、取回、撤回、催办、转发旁支、关注和系统自动节点；每项验证 enabled、禁用原因、参数和重读要求。
- 场景编译单元测试：重复动作记录、拖拽顺序、同意后取回、取回前准备、驳回/撤回后重提、回退后重走、移交、加签重读、转发不移动主游标、最终导航及不可恢复顺序拒绝；确认不创建第二主实例。
- 回放任务单元/集成测试：计划默认与路径覆盖、单活动任务、路径明细独立终态、取消/恢复、租约、游标分页、幂等、换路原子性和目标写调用为零。
- API 契约测试：F-012 新接口的归属、修订号、`Idempotency-Key`、敏感字段、旧接口 404；公开响应不包含 SID、凭证、完整目标正文、目标内部 ID、代理 ID 或临时任务 ID。
- MySQL 集成测试：核对测试工具数据库后清空业务表，保留 `schema_migrations` 和运行时维护表；验证新表、外键、唯一活动任务、锁与重启读取；明确证明目标平台数据库不被连接或修改。
- 数据运行时测试：FormMaking 真实模板完整回填、已注册组件、权限 refresh、`getData(true)`、`getValues()`，以及自定义页面历史状态/业务对象捕获、字段绑定歧义、只读请求策略、历史恢复、人工条件字段编辑和 iframe 会话销毁；不启动旧生成业务桥接。
- 前端结构测试：历史候选弹窗、默认/路径覆盖、FormMaking/自定义表单差异与缺失提示、换路确认、动作目录/禁用原因、动作排序与编译预览、旧生成/规则/循环入口消失且自定义数据入口保留。
- 注释与边界测试：新增或修改 Go 具名函数和方法具备紧邻中文职责/边界注释；核心事务、并发、幂等、敏感数据和目标只读边界有说明。
- `test/run-f012.sh` 只聚合上述当前范围测试、必要 Go 检查、主前端和 form-runtime 类型检查/构建及 `git diff --check`；不引用旧 F-008 至 F-011 脚本，不自动启动浏览器。
- 用户按 F-012 功能文件的人工清单核对真实复杂 FormMaking、自定义表单、历史回放、换路、动作门禁、编译顺序、刷新恢复和目标平台零写入。
