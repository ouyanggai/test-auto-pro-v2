# 路线图

## 阶段全景

| 阶段 | 目标 | 状态 |
| --- | --- | --- |
| F-000 | 项目初始化：文档、技能、参考源码、前后端最小骨架与运行工具 | accepted |
| F-001 | 经人工确认的应用壳视觉与测试计划页静态 mock | accepted |
| F-002 | 真实账号登录与三类目标平台只读列表闭环 | accepted |
| F-003 | 引入 MySQL，持久化最小计划并提供继续配置入口 | accepted |
| F-004 | 使用 Vue Flow 显示只读真实流程树 | accepted |
| F-005 | 在真实流程结构上选择并持久化执行路径 | accepted |
| F-006 | 分析所选路径的条件、人员和可配置动作要求 | accepted |
| F-007 | 在已保存路径的独立画布中配置人员策略和路径表单数据；原动作配置方案不进入人工验收 | archived |
| F-008 | 节点动作、路径准备与全路径后台解析 | ready_for_manual |
| F-009 | 批量路径准备与智能表单生成 | ready_for_manual |
| F-010 | 全模板规则目录、Vue 业务表单与自定义组件生成能力 | implementing |
| F-010R | F-010 全量目录权限过滤与 Vue/Java 规则适配返工 | implementing |
| 后续 | 扩展运行预检、真实执行和运行记录 | 未规划 |

## 当前与后两项

### F-003 最小计划持久化（已验收）

引入 MySQL，将新建页已经确认的计划名称、账号、流程来源、目标对象、串并方式和可选定时时间保存为“未运行”计划；计划列表改读真实数据，并提供可恢复的继续配置入口。

详细范围、MySQL 边界、API、验证和人工门禁见 `docs/features/F-003-minimal-plan-persistence.md`。用户已确认计划可正常读取，F-003 于 2026-07-28 进入 `accepted`。

### F-004 只读真实流程图（已验收）

使用已保存计划的账号、来源和目标对象重新核对当前可见记录；新发起读取模板树，已发和待发读取各自实例代理树。使用 Vue Flow 和流程专用树布局，只读显示真实节点、条件/手动分支、并行、嵌套与汇合，提供平移、缩放、页面全屏、适配以及稳定错误反馈。

详细范围、来源接口、图映射、页面交互、自动验证和人工门禁见 `docs/features/F-004-read-only-real-flow-graph.md`。用户于 2026-07-28 要求开始下一项任务，F-004 进入 `accepted`。

### F-005 执行路径选择与持久化（已验收）

在已核实的真实流程结构上逐条选择条件/手动分支，并把并行分支作为同一路径的全部必经线路；首次建立路径表与分支选择表，支持完整路径新增、编辑、复制和删除。新发起可保存多条，已发/待发因当前计划只绑定一个实例而最多一条。

详细范围、路径语义、当前节点入口、数据表、API、画布交互、验证和人工门禁见 `docs/features/F-005-execution-path-selection.md`。普通画布只保留文字页面全屏按钮，页面全屏后才显示路径列表、受来源/数量限制的新增入口和文字退出按钮；用户于 2026-08-07 明确要求开始下一项任务，F-005 进入 `accepted`。

### F-006 路径要求分析（已验收）

读取所选路径上的真实条件、人员规则和标准节点动作，形成供后续配置直接消费的只读要求结果。独立核对页面、入口和页面专用前端代码已删除，后端分析与 API 保留并通过聚合验证；用户已明确继续下一任务，F-006 进入 `accepted`。详细范围见 `docs/features/F-006-path-requirements-review.md`。

### F-007 路径节点与独立表单数据配置（已归档）

保留 F-005 已通过验收的路径交互和独立 FormMaking 工作区。节点人员、表单工作区、分支提示和字段保护继续作为既有范围；用户已明确归档 F-007，其动作配置方案由 F-008 完整替代，不再返工或进入人工验收。详细范围见 `docs/features/F-007-path-data-action-configuration.md`。

### F-008 节点动作、循环与批量准备（等待人工验收）

将节点级动作和路径级循环拆成两个独立入口，并增加面向多路径的准备情况、一键配置和本次测试路径选择。动作配置保留目标工作台已验证的动作语义；循环只使用引擎真实支持的“不同意后重提”或“上一节点返工”，不再伪造任意回退到起点。旧动作模型不再与新模型混用，F-005、人员、表单和目标写入边界不变。详细范围、状态机、验证与人工门禁见 `docs/features/F-008-node-action-cycle-configuration.md`。

### F-009 批量路径准备与智能表单生成（返工完成，等待人工验收）

已把 F-008 的同步动作预设替换为独立、持久化、可取消和恢复的后台准备任务。节点配置状态与表单数据状态已经拆开；任务只处理勾选路径，在同一真实流程快照下保留人工配置、补齐安全节点默认值并确定性求解完整路径表单。本轮补充服务端当前处理路径、单块真实进度、完成后刷新再收起、勾选不触发定位，以及 `not_started/running/completed` 三态计划边界。全模板适配与本地规则目录由 F-010 接续，模板范围只取目标实时分页总数，不设置固定上限。详细范围见 `docs/features/F-009-batch-path-preparation.md`。F-009 和 F-008 均停在 `ready_for_manual`，未被自动验收。

### F-010 全模板规则分析与 Vue 表单流程（复核返工中）

系统设置新增本地模板规则目录，以“欧阳改”账号执行增量、全量或失败重试分析；目标流程条件、FormMaking 模板、宿主 Vue 页面、Java 协议和全部已注册自定义组件形成同一持久规则快照。计划配置与批量任务只消费该目录并按计划发起人过滤。2026-08-24 真实任务读取实时总数 735，成功列出 734 项，唯一不可读目标槽位计入 `unlisted=1`，任务对账完成并显示 100%；目录为 604 完整、23 需处理、107 阻断、0 失败。F-010 全部定向测试、Go 测试、MySQL 集成、主前端与 form-runtime 类型检查和双构建通过，状态停在 `ready_for_manual`。详细范围见 `docs/features/F-010-template-rule-analysis-vue-flow.md`。

### F-010R F-010 全量目录权限过滤与 Vue/Java 规则适配返工（复核返工中）

本返工明确“欧阳改只负责建立全量规则目录，计划使用时按发起人账号过滤模板、规则、身份和候选权限”。2026-08-24 复核返工中的版本管理方案已被用户覆盖：不建立规则/候选版本或草稿版本，只由系统设置批量检测目录规则是否过期；计划页只消费本地 `stale` 状态并提示先更新，设置页提供“一键更新待更新模板”。近期样本空值/失败负缓存和同键单飞继续保留。F-010 与 F-010R 保持 `implementing`，详细台账见 `docs/features/F-010-template-rule-analysis-vue-flow-rework-plan.md`。

### 后续运行预检与真实执行

检查路径配置完整性，生成独立运行记录并调用目标平台真实接口执行；具体切片不在 F-006 预判实施。

## 推进规则

当前功能到达 `ready_for_manual` 后停止。只有用户明确验收并批准下一项，路线图才能推进；规划完成后必须停在 `awaiting_approval`，不能因用户要求“继续下一个任务”直接开始编码。
