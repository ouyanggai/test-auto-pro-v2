# 路线图

## 阶段全景

| 阶段 | 目标 | 状态 |
| --- | --- | --- |
| F-000 | 项目初始化：文档、技能、参考源码、前后端最小骨架与运行工具 | accepted |
| F-001 | 经人工确认的应用壳视觉与测试计划页静态 mock | accepted |
| F-002 | 真实账号登录与三类目标平台只读列表闭环 | accepted |
| F-003 | 引入 MySQL，持久化最小计划并提供继续配置入口 | accepted |
| F-004 | 引入 Vue Flow + dagre，显示只读真实流程 | preparing |
| F-005 | 在真实流程结构上选择并持久化执行路径 | 未开始 |
| 后续 | 按每批不超过 5～8 项扩展路径配置、运行和表单适配 | 未规划 |

## 当前与后两项

### F-003 最小计划持久化（已验收）

引入 MySQL，将新建页已经确认的计划名称、账号、流程来源、目标对象、串并方式和可选定时时间保存为“待配置”计划；计划列表改读真实数据，并提供可恢复的继续配置入口。

详细范围、MySQL 边界、API、验证和人工门禁见 `docs/features/F-003-minimal-plan-persistence.md`。用户已确认计划可正常读取，F-003 于 2026-07-28 进入 `accepted`。

### F-004 只读真实流程图（当前）

使用已保存计划的账号、来源和目标对象重新读取真实流程结构，引入 Vue Flow + dagre 只读显示真实节点与分支。

### F-005 执行路径选择与持久化（后两项）

在已经核实的真实流程结构上选择执行路径，首次建立路径表和路径配置保存；不在 F-003 预存空路径或伪路径。

## 推进规则

当前功能到达 `ready_for_manual` 后停止。只有用户明确验收并批准下一项，路线图才能推进。
