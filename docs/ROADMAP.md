# 路线图

## 阶段全景

| 阶段 | 目标 | 状态 |
| --- | --- | --- |
| F-000 | 项目初始化：文档、技能、参考源码、前后端最小骨架与运行工具 | accepted |
| F-001 | 经人工确认的应用壳视觉与测试计划页静态 mock | accepted |
| F-002 | 真实账号登录与三类目标平台只读列表闭环 | implementing |
| F-003 | 引入 MySQL，持久化最小计划与路径配置 | 未开始 |
| F-004 | 引入 Vue Flow + dagre，显示只读真实流程 | 未开始 |
| 后续 | 按每批不超过 5～8 项扩展路径配置、运行和表单适配 | 未规划 |

## 当前与后两项

### F-002 真实账号与只读列表闭环（当前）

在 F-001 已验收界面内接入真实账号登录，并按新发起、已发、待发读取目标平台真实流程对象；保持只读，不保存计划。

详细范围、接口、安全边界与真实环境前提见 `docs/features/F-002-target-platform-read.md`。当前正按截图反馈移除错误的公司目录查询，改为高亮模板已有的 `groupName`，并修正已发状态中文展示；完成验证后才会重新停在 `ready_for_manual`。

### F-003 最小计划持久化（下一项）

在真实账号与流程读取边界验收后，引入 MySQL 持久化最小计划与路径配置。

### F-004 只读真实流程图（后两项）

在真实读取和最小持久化均验收后，引入 Vue Flow + dagre 显示只读真实流程结构。

## 推进规则

当前功能到达 `ready_for_manual` 后停止。只有用户明确验收并批准下一项，路线图才能推进。
