# 浏览器验证与返工执行报告

任务书：docs/auto/2026-09-09-browser-verification-repair-task.md
开始时间：2026-09-09
状态：进行中（总体未完成，不写收口）

## 0. 现场记录

- 基线提交：1fe7efa 文档：新增真实网页验证与功能返工任务书
- 未提交改动（任务书 2.3 列出的 8 个文件 + 1 个删除）：
  - form-runtime/src/App.vue、runtime/formTemplate.js、runtime/protocol.js、runtime/requestPolicy.js
  - internal/adapter/target/write_actions.go（其中 `approve` 同意动作载荷改动与表单遮罩无关，单独审查验证提交）
  - test/unit/frontend/form_runtime_test.mjs
  - web/src/features/path-configuration/FormRuntimeFrame.vue
  - web/src/views/PlanPathConfigurationView.vue
  - docs/CLAUDE_CODE_FORM_LOADING_FIX.md 已删除（保持删除，不恢复文件）
- 用户反馈：未提交改动导致“表单数据显示一直在加载中，都显示不了了”。
- 服务现场：19000（web）、19001（form-runtime dev）、19080（backend via air）均已在运行，health 正常；沿用，不重复起服务。
- 本机被忽略配置 .env.local 存在，沿用，不打印配置值。

## 1. 样本清单（按任务书 4.2 登记，实际名称/编号从网页读取）

计划：浏览器返工验证-合同盖章评审-20260909（plan_id=25，新发起，串行，发起账号骆蒙恩 · 广东斯能投资有限责任公司，流程合同盖章评审（广东斯能），共 505 条路径）

| 样本号 | 计划及路径 | 真实分支组合 | 历史来源 | 关键字段前后值 | 动作链 | 节点处理人 | 模式 | 运行定位 | 结果/证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| H01 | 25/4173（H01-施工类50万-深圳，画布手动新建） | 分支1→EPC工程、施工类、工程服务类、设备采购类、运维类→施工类>50万→嵌套分支1→盖章地=深圳 | 天安数码城AI智能园区-10000元（胡胜梅，2026-03-05） | 分类：行政综合类→施工类；小写：10000→500001；盖章地：→深圳 | 保存草稿+20×同意，编译器补1次重新提交 | 21 节点随机范围选人（待运行核对实名） | 单步（E01-E12 控制实验） | 待运行 | 配置完成 |
| H02 | 25/3672（路径 4） | 同上分类分支→工程服务类>20万→盖章地=太原 | 同上（批量来源） | 分类→工程服务类（待运行核对） | 保存草稿+同意链 | 随机范围选人 | 自动 | 待运行 | 配置完成 |
| H03 | 25/3673（路径 5） | 设备采购类>20万→深圳 | 同上 | 待运行核对 | 保存草稿+同意链 | 随机范围选人 | 自动 | 待运行 | 配置完成 |
| H04 | 25/3679（路径 11） | EPC工程类>5000万→深圳 | 同上 | 待运行核对 | 保存草稿+同意链 | 随机范围选人 | 自动 | 待运行 | 配置完成 |
| H05 | 25/3705（路径 37） | 投资类→投资类、运维类、非常规类→深圳 | 同上 | 待运行核对 | 保存草稿+同意链 | 随机范围选人 | 自动 | 待运行 | 配置完成 |
| H06 | 25/3731（路径 63） | 并网类、行政综合类、非常规类→行政综合类>5万→深圳 | 同上 | 待运行核对 | 保存草稿+同意链（19 动作） | 随机范围选人 | 自动 | 待运行 | 配置完成 |
| H07 | 25/3681（路径 13） | EPC等类→其他条件（金额兜底）→深圳 | 同上 | 待运行核对 | 保存草稿+同意链（20 动作） | 随机范围选人 | 自动 | 待运行 | 配置完成 |
| H08 | 25/3734（路径 66） | 并网等类→投资类、运维类、非常规类→太原 | 同上 | 待运行核对 | 保存草稿+同意链（19 动作） | 随机范围选人 | 自动 | 待运行 | 配置完成 |
| H09 | 25/3706（路径 38） | 投资类→投资类、运维类、非常规类→太原 | 同上 | 待运行核对 | 保存草稿+同意链 | 随机范围选人 | 自动 | 待运行 | 配置完成 |
| H10 | 25/3738（路径 70） | 并网等类→其他条件→太原 | 同上 | 待运行核对 | 保存草稿+同意链（18 动作） | 随机范围选人 | 自动 | 待运行 | 配置完成 |
| O01 | 待建计划（oyg流程测试） | 待读取 | 待选择 | 待核对 | 待配置 | 待核对 | 待安排 | 待运行 | 未开始 |
| L01 | 待建计划（员工请假单（智慧斯能）） | 待读取 | 待选择 | 待核对 | 待配置 | 待核对 | 待安排 | 待运行 | 未开始 |

说明：plan 25 的路径 3669（路径 1）在修复前由旧一键配置产物配置（含暂存/取回/不同意/回退等不可流转动作），已用画布手动新建路径 4173（同分支组合）替代其 H01 样本位置；3669 不勾选运行，保留作为缺陷对照证据。

## 2. 问题记录（按任务书 12.2 模板逐个登记）

### 问题 1：表单数据工作区一直显示"正在加载表单运行时"，最终表单被销毁（工作包 A）

- 关联工作包/功能：F-024 表单装载生命周期；任务书工作包 A。
- 网页复现入口和点击步骤：测试计划 → oyg测试102 → 编辑 → 路径 1「配置节点」→「表单数据」。
- 预期：遮罩在总预算内关闭，表单可见、可保存。
- 实际：遮罩停留 60 秒后出现「表单 iframe 初始化响应超时」，iframe 会话被销毁只剩占位文案；此前表单内容实际已渲染完成（约 1.7 秒内网络请求全部结束）。
- 根因与证据（确认事实）：
  1. 父↔iframe 消息时序实测：boot 后 60.076 秒才收到 load 的 result，恰好晚于父页面 60 秒预算；父页面按预算销毁会话（postMessage 记录 + 页面快照）。
  2. iframe 内资源时序实测：73 个目标请求全部在约 1.7 秒内完成，最慢 430ms，"分钟级慢请求"假设不成立（performance.getEntriesByType）。
  3. 分段探针定位：卡点在 `refreshOptionFields` → FormMaking `refreshFieldOptionData` 的 `Promise.all` 内部，且挂起期间请求屏障 pending=0（无在途请求）。
  4. 目标源码核对：FormMaking `refreshOptionData` 只有 `fx` 与"带数据源键的 datasource"两个落定分支；合同盖章评审表的身份字段 `currentDepartment` 声明 `remote=true, remoteType=datasource` 但 `remoteDataSource=null`，走不到任何分支，返回永不落定的 Promise，拖死整次装载。
  5. 配置数据接口读取模板证实该控件形状（GET /api/plans/24/execution-paths/3165/configuration/data）。
  - 待验证假设：无（根因链完整）。
- 修改文件和行为：`form-runtime/src/runtime/formTemplate.js` 的 `refreshOptionFields` 现在只刷新 FormMaking 能落定的控件（datasource 型必须带数据源键、fx 型、模板未声明类型的简单 remote 控件），并给整次刷新调用加 10 秒有界兜底；`optionFieldDescriptors` 透传 `remoteType/remoteDataSource/remoteFx`。此类身份控件的选项来自宿主回显与联动，不依赖该刷新。
- 相似场景检查：既有 53 项表单运行时用例全部通过；新增回归用例锁定"缺数据源键的 datasource 控件不得被刷新、不得拖死装载"。
- 运行的测试/构建及结果：`node --experimental-strip-types --test test/unit/frontend/form_runtime_test.mjs` 53/53 通过；web 与 form-runtime typecheck、生产构建通过；`git diff --check` 干净。
- 浏览器复验结果：原失败路径装载 8.3 秒内完成，遮罩关闭、保存/恢复按钮可用；关闭重开、浏览器整页刷新后重开均复现一致；iframe 内不再出现占位或超时错误。
- 计划/路径/运行/步骤定位：计划 24（oyg测试102）路径 3165（路径 1）。
- 截图或日志的本地证据位置：本报告第 0、2 节记录（postMessage 时序与探针数据已在修复前采集）。
- 中文提交：见第 3 节提交列表第 1 项。
- 当前状态：已验证。
- 附带发现并已修复：19001 form-runtime dev server 文件监视失效、长期供给陈旧产物（验证浏览器消费的构建产物时发现），已重启并确认新产物被浏览器消费；重编译暴露 `10_000` 数字分隔符语法不被本仓库 Babel 支持，已改为普通数字字面量。

### 问题 2：合同分类显示值与路径条件不一致（控件显示历史分类"行政综合类"，右侧补丁为"施工类"）（工作包 B）

- 关联工作包/功能：F-024 关键字段协调；任务书工作包 B。
- 网页复现入口和点击步骤：同问题 1（路径 1 的分支组合即施工类>50万、盖章地=深圳）。
- 预期：控件选中值、显示名称、右侧路径关键信息三者一致为"施工类"。
- 实际（修复前）：右侧显示施工类，控件停留行政综合类；协调循环反复回填-被覆盖，装载时间被拖长并触发问题 1 的超时链。
- 根因与证据：问题 1 的装载挂死是本问题的主要放大器——协调完成前会话已被销毁，任何已取得的修正都看不到。挂死移除后协调自然收敛（确认事实：修复后控件显示施工类）。
- 修改文件和行为：本问题未新增独立修改；验证随问题 1 的修复完成。
- 相似场景检查：关闭重开、浏览器刷新后重开，控件均为施工类，无自触发反复改值。
- 运行的测试/构建及结果：同问题 1。
- 浏览器复验结果：控件"施工类" = 右侧路径关键信息"施工类" = 保存后重读一致。
- 六处事实核对进展：历史快照（行政综合类，来自历史实例）、路径补丁（施工类）、运行时控件（施工类）、运行时取值（保存通过）已核对；服务端重读与真实目标实例的提交值待运行阶段（H01）核对。
- 当前状态：已验证（配置态）；目标写入一致性归入 H01 运行证据。

### 问题 3：单步放行会重复执行已落账步骤（工作包 E01/E02 缺陷）

- 网页复现：H01 首次单步运行中，导航步骤落账后再次点击「放行」会重复执行同一步（stepNo=3 出现两条成功记录，doneSteps 虚增为 4）。
- 根因（确认事实）：下一步预览因目标结构读取瞬断构建失败时游标不前进；导航步骤落账无领取推进权保护，放行可重复落账。写步骤同样存在重复写风险。
- 修复：`approveOneStep` 对预览构建加有界退避重试；`reserveApproval` 加防重护栏（已落账步骤的 step 命令拒绝并返回中文错误）；回归测试锁定。
- 中文提交：9bea807。
- 当前状态：已验证（护栏单测 + 修复后运行无重复落账）。

### 问题 4：导航步骤落账后状态冲突导致游标卡死（阻断长路径）

- 网页复现：路径 4173 的第 3 步（只读导航）落账后，`BackToRunning`（核验中→运行中）必然撞「运行中→运行中」冲突，RunApprovedStep 返回错误使游标永远停在导航步——合同流程含 7 个导航步，长路径必然卡死。
- 根因（确认事实）：只读导航步骤没有核验中阶段，落账后状态仍是运行中；状态迁移把"已是运行中"当成非法冲突。
- 修复：`BackToRunning` 幂等容忍"已是运行中"（重读确认后返回成功）。
- 中文提交：5e2a4b2。
- 当前状态：已验证（修复后导航步骤正常推进到第 4 步）。

### 问题 5：一键配置生成的动作链不可流转（工作包 C）

- 网页复现：plan 25 路径 3669 一键配置产物含暂存/取回/不同意/回退等动作；暂存不推进流程、取回拉回已办、不同意重走全链时上游动作已消费——首个导航步之后必然停住。
- 根因（确认事实）：`autoNodeActionCandidates` 按动作种类覆盖轮转选择，未约束节点动作必须让主实例离开。
- 修复：一键配置只在可流转动作（同意/保存草稿/重新提交/撤回）中选择；目录中无可流转动作时节点保持待配置阻塞运行。H02—H10 用修复后产物全部为"保存草稿+同意链+1 次重提交恢复"。
- 中文提交：f993789。
- 当前状态：已验证（编译场景核查 9 条路径零非法动作）。

### 问题 6：执行器所有步骤都用计划账号会话（工作包 D 核心缺陷）

- 网页复现：H01 审批到达资源开发部节点时事实读取持续「SID已失效」，重登录互踢循环；curl 日志证实所有登录都是骆蒙恩。
- 根因（确认事实）：`sessionWithRetry` 恒用 `runCtx.PlanAccount`；执行器没有按节点人员切换账号的任何逻辑。
- 修复：任务列表事实新增 `currentPendingUserId/Name`（目标裁决的实际处理人）；任务级动作写前把演员会话切换为实际处理人（人员目录 account 字段解析登录账号并登录）；解析失败如实置败不回退计划账号。回归测试锁定"审批写请求必须以实际处理人会话发出"。
- 中文提交：c01032e。
- 当前状态：已验证（单测）；真实目标上的完整审批链验证进行中。

### 问题 7：无法只运行部分路径（S01 缺口）

- 网页复现：计划列表「运行」永远自动运行全部就绪路径；路径页勾选框只服务一键配置。
- 修复：运行前检查对话框增加可运行路径勾选区（默认全选）；存在阻塞路径时允许运行就绪子集。
- 中文提交：29140a0。
- 当前状态：已验证（对话框显示 7 条可运行、取消后计数与启动范围正确）。

### 问题 8：骆蒙恩账号触发目标会话限制（外部约束）

- 现象：运行中事实读取持续「SID已失效」，新发 SID ~2 秒即死；重登录雪上加霜。
- 根因（确认事实）：多轮运行中每次写都被目标拒一次会话再重发，登录频率过高触发目标平台账号级会话限制（与 executor.go 内 2026-09-07 实测注释一致）。
- 缓解：连续会话刷新之间强制退避（1s/2s/3s 递增）；演员切换把登录压力分散到各节点处理人账号。等待目标侧限制解除后继续验证。
- 中文提交：873c8ed。
- 当前状态：外部受阻（等待目标账号冷却），已带缓解措施。

### 问题 9：运行页路径标签点击后面板不刷新

- 网页复现：多路径运行面板点击另一路径标签，URL 已切换但面板仍显示旧路径；手动刷新后才正确。
- 根因：`selectedPathRunID` 只在初始化与首次缺省解析时赋值，无路由参数监听。
- 修复：监听 `route.params.pathRunId` 变化重读详情。
- 中文提交：873c8ed。
- 当前状态：已验证。

### 遗留观察（未修复，如实记录）

- plan 25 有 4 条路径（3672/3706/3734/3738，对应太原盖章地与部分金额分支）数据状态 needs_input：以深圳"天安数码城"实例为历史来源时，太原盖章地与部分金额分支无法形成可复验补丁（阻断信息已精确到字段与路由）。处理方向：为太原分支路径更换太原地区历史来源实例。
- 运行 52 第 2 步 reSubmit 被目标以「空指针异常」拒绝（载荷与成功运行完全一致、仅实例 ID 不同），判定为目标环境抖动；产品按确定失败停止符合语义。

## 3. 验证与提交记录

### 提交列表

1. （本次）修复表单装载挂死：跳过 FormMaking 永不落定的选项刷新控件并加有界兜底；接手表单遮罩生命周期未提交改动（含取消命令、会话代次、总预算与 iframe 屏障）；移除已被取代的旧修复文档。
2. （本次）补齐同意动作载荷：approve 分支与发送前校验（auditStatus=pass、实例/待办必填），含回归测试；与表单修复分开提交。

### 验证命令与结果

- `node --experimental-strip-types --test test/unit/frontend/form_runtime_test.mjs`：53/53 通过（0 跳过）
- `npm run typecheck`（web、form-runtime）：通过
- `npm run build`（web、form-runtime）：通过
- `go test -count=1 ./test/unit/backend/executor ./test/unit/backend/target ./test/unit/backend/action_orchestration`：通过
- `git diff --check`：干净

## 4. 阶段性结论（截至本轮窗口结束，总体未完成，不写收口）

### 已完成并有证据的修复（9 项，中文原子提交）

1. d5f14c7 表单装载挂死修复（工作包 A）：根因是 FormMaking 对缺数据源键的 datasource 控件返回永不落定 Promise；修复后路径 4173 表单 8 秒可用，分类控件与右侧补丁一致（施工类）。
2. aec2f44 同意动作载荷补齐（F-019 缺口）。
3. f993789 一键配置只选可流转动作（工作包 C）。
4. 9bea807 防重复放行护栏。
5. 5e2a4b2 导航步骤游标卡死修复（长路径阻断）。
6. c01032e 执行器按实时待办处理人切换会话（工作包 D 核心）。
7. 29140a0 S01 部分运行勾选。
8. 873c8ed 会话刷新退避 + 运行页标签刷新修复。
9. 6909c5d 报告补记。

### 真实执行进展

- H01（路径 4173）多次真实运行：第 1 步保存草稿真实写入成功（实例 101b5429…、9ec04dd2…、75658324… 等），第 2 步重新提交成功，第 3 步只读导航落账通过；E01/E02（单步推进、防重复点击）均有实证；串行调度推进、路径级停止终态、S04 隔离均有实证。
- 卡点（当前阻塞）：第 4 步（资源开发部审批）的门禁事实读取以计划账号查 pending 待办列表持续 RESP401「SID已失效」或返回不含本实例任务的全公司待办分页（79-100 行、无本实例），两种形态都使 CurrentTaskFound=false → 审批被门禁阻断。
- 待验证假设（按优先级）：
  1. H-可见性：reSubmit 只携带了分支 nodeProxyId 的 nextAuditorList（实测载荷），目标为下一审批节点生成待办时因缺少/未携带实际处理人或业务关联而未建任务——实例停在资源开发部但无待办；需对照 FlowOperateServiceImpl.reSubmit → configAuditPersonnel 的建任务链。
  2. H-读取语义：flowJobTaskLink/list+pending 是"个人待办"语义，计划账号本来就读不到他人待办；修复方向是把任务级步骤的门禁读取切到配置的处理人会话（需要先解决固定规则节点的处理人预知问题）。
  3. H-限流残留：此前高频登录触发的会话限制未完全解除（部分证据：同 SID 其他端点正常、本端点 100% 失败，倾向排除）。
- 下一步（继续工作的入口）：先做 H-可见性验证——运行后直接用 getCurrentFromData 读实例当前节点（已证可行），再用 nextAuditorList 携带真实处理人（RunContext.NextNodeAuditors 已有解析链）重试 reSubmit；若任务出现，则完成 E 系列。
- 5e2a4b2/9bea807/c01032e 三个修复已让运行引擎从"必然卡死"推进到"稳定推进到首个审批节点"；剩余是目标建任务语义。

### 样本状态

- H01：配置就绪；执行推进到 4/29（首个审批节点）被上述阻塞。
- H02—H10：配置就绪（编译场景核查通过），未运行。
- H02 部分/太原分支等 4 条路径（3672/3706/3734/3738）needs_input：需换太原地区历史来源（见第 2 节遗留观察）。
- O01/L01：计划未创建（账号会话限流期间避免对目标加压；创建入口与流程候选已就绪：员工请假单（智慧斯能）等模板可在新建计划页直接选择）。
