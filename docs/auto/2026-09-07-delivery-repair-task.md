# 交付缺口返工任务（继续执行）

## 结论

上一轮报告不通过，不能沿用“已收口”的结论。报告自己已经承认十个样本没有完整跑通、动作配置删除未生效、真实多步推进与断点/循环没有证据；当前代码还存在执行链结构性缺口。继续从当前工作区修复，不重做已验证内容。

本轮以业务代码基线 `11562e1` 开始，持续执行到 **2026-09-07 08:55（Asia/Shanghai）**。不要等待人工审核；到时仍未完成就如实提交已验证成果和剩余项，禁止虚报完成。

## 已确认缺口

1. `docs/features/F-019-full-action-and-recovery-execution.md` 第 205—212 行已明确记录六个未修复缺口，但验收报告仍写“行为修复与链路修复全部完成”。先纠正报告状态。
2. `internal/engine/step/gate.go` 的 `buildRequest` 没有 `save_draft`、`resubmit` 可达分支；多个任务动作没有发送前实时补齐 `jobTaskId`。
3. `internal/engine/step/types.go` 与 `executor.go` 仍把 `PlanAccount` 当所有动作的执行人；已保存的 `person_strategies` 没有进入运行上下文和执行链。
4. `internal/adapter/target/write_actions.go` 的移交/加签固定发送空 `userIds`，也没有承接目标前端真实使用的 `batchNo`；转发接收人只读动作参数。
5. `run_scheduling.go` 把计划目标实例 ID 同时当 `FlowProxyID` 使用，实例、流程代理、表单代理和任务身份没有分清。
6. `form-runtime/src/runtime/formTemplate.js` 会删除 `beforeSubmitAndDraft`、`beforeSubmit`，现有链路也没有执行目标页面中的 `beforeSubmitAndDraftNoBiz`、`afterSaveFlowInstance` 等业务生命周期处理。只调用流程端点不足以等价于目标页面真实提交。
7. 动作配置页面删除后保存未生效，上一轮报告已承认未定位完成。
8. 运行节点卡片目前只是固定小卡片加圆角、阴影、状态点和外圈动画；卡片没有自然呈现当前动作、处理人、步序和耗时，连线也未在“画布质感”提交中重做。整卡脉冲还违背“不让整张卡片跳动”的要求。
9. 右栏仍显示主实例内部 ID，复制失败会静默；错误详情缺少完整冻结配置和明确缺失原因。
10. `test/run-f019.sh` 主要跑旧切片回归与字符串契约；F-019 只有少量直接载荷测试，不能证明 15 个动作经过“门禁→人员→实时任务→请求→响应→重读→落账”。
11. “账号级会话限制，不是工具缺陷”目前只是推断。现代码每次写前强制重登，鉴权拒绝后同一尝试最多重发三次，本身可能制造互踢或重复写风险；未取得目标源码和请求证据前，按工具问题处理。

目标平台前端和后端源码足够覆盖静态协议、前置条件、特殊生命周期和响应分支；真实权限、数据状态、部署差异与会话时序仍必须用真实运行验证，不能只靠读源码宣布通过。

## 固定边界

- 本轮不做 F-023，不改发布、Docker、Compose、镜像和部署文档。
- F-012 已验收，不重复做；只修复它与真实执行直接相连的动作保存、人员快照和运行时读取。
- 忽略的是项目文档中的中途人工门禁，不是目标平台的业务门禁、写入安全和一次写纪律。
- 不修改目标平台源码或数据库，不伪造目标响应，不靠延时、吞错、盲目重试或换当前登录人绕过问题。
- 优先使用现有模型和存储，不新增第二套动作定义、执行器、画布或运行详情页，不做无关重构。
- 测试放在根目录 `test/` 分类目录；每个原子修复在验证后单独中文提交，只本地提交，不提交凭证和运行日志。

## 执行顺序

### 1. 先建立目标动作真值表

逐个追踪下列 15 个动作：保存草稿、提交、重新提交、暂存表单、加签、移交、同意、不同意、回退、取回、撤回、催办、转发、关注、取消关注。

以 `参考代码/rsh-cloud-invest-power-system/src/views/GroupApproveManage/` 及其直接公共组件、`参考代码/rsh-cloud-invest-power-system/src/api/index.js` 和 `参考代码/java-serve/` 为依据，生成 `docs/auto/2026-09-07-target-action-matrix.md`。每个动作必须写清：

- 页面入口和可用条件；
- 前置/后置业务钩子及执行顺序；
- 执行账号、目标人员和实时任务从哪里取得；
- 端点、完整请求字段及每个字段来源；
- 成功、业务拒绝、鉴权拒绝、响应异常的真实结构；
- 写后要重读什么事实，什么结果才算确定成功。

不要只抄接口名。必须从按钮处理函数一路追到后端控制器和服务实现。字段或语义没找到就标成阻塞，不允许猜。

### 2. 先修配置保存，再修执行

- 真实复现“删除动作→保存→刷新/重新进入后动作仍存在”，定位是子组件事件拆分、父页面草稿、保存请求还是服务端重编译覆盖。
- 修复节点动作和实例动作的新增、删除、排序、次数、人员选择保存；保存响应后重读，页面与数据库必须一致。
- 补前端逻辑测试和后端保存集成测试，并用真实页面完整复验。

### 3. 分清身份并接通人员解析

- 在运行上下文中明确区分目标实例 ID、流程代理 ID、表单代理 ID、真实节点 ID、待办/已办任务 ID、批次号；禁止一个字段多种含义。
- 把当前路径已保存的人员策略冻结进本次运行，并在每个动作 `prepare` 阶段解析真实执行人或接收人。
- 覆盖固定人员、岗位、角色、部门、公司、表单人员、发起人、运行时选择，以及移交后的新处理人；需唯一人员时不唯一就明确阻塞，不能回退到计划账号。
- 执行动作使用实际处理人的会话；发送前按实例、真实节点和处理人读取最新待办/已办，补齐 `jobTaskId`、`batchNo`、`userIds`、`receiverId` 等字段。
- 逐策略补单元测试和集成测试，断言执行账号、接收人和请求载荷三者一致。

### 4. 按真值表补齐 15 个动作和业务生命周期

- 让 `save_draft`、`resubmit` 真正进入对应请求分支；任务级动作必须现场补任务事实，实例级动作不能被错误节点门禁挡住。
- 每个动作独立实现前置条件、专用载荷、响应判定和写后重读，不能让通用 `isSuccess` 掩盖特殊响应。
- 移交、加签、转发必须发送用户真正配置且实时可用的人员；禁止空 `userIds`。
- 重提、取回、回退后的恢复步骤要有独立步骤、事实和日志；最终导航必须有真实判据，不能复用“同意”假装成功；转发辅助流程要与主实例分开登记。
- 对 FormMaking 和 Vue 自定义表单，按目标页面的真实调用顺序补齐业务生命周期。不要在 Go 中执行任意模板脚本；根据参考代码建立受控、可审计的白名单适配，至少覆盖目标实际使用的提交/草稿前置处理、无业务处理、流程创建后绑定和回退同步。未知钩子必须明确阻塞并指出名称，不能删除后假装已支持。
- 每完成一个动作，都要有“参考源码位置→当前实现→自动测试→真实运行证据”的闭环，再标记通过。

### 5. 修正会话、重试和结果判定

- 从目标网关/鉴权源码和真实请求日志确认 SID 的失效规则，先证明原因再改策略。
- 不要每个写动作无条件强制重登；同一执行人优先复用经探活确认的会话，人员切换才切换会话。
- 请求是否进入业务层不明确时绝不重发。只有目标协议能明确证明“鉴权阶段拒绝且业务未执行”时才允许刷新会话重试，并把每次尝试完整记录。
- 为 15 个动作分别定义成功、不变、推进、辅助流程创建、业务拒绝、结果不确定的重读判据；不能把“不变类动作”一律判不确定，也不能无判据判成功。
- 没有充分证据时按工具问题或结果待确认处理，不能写“目标限制，不是工具缺陷”。

### 6. 真正重做运行画布和诊断体验

- 先让后端运行快照提供准确的当前动作、实际处理人、步序、耗时、错误、冻结配置，再改前端；禁止只改 CSS。
- 重做节点卡片的信息层级：节点名为主信息，状态、当前动作、处理人、步序/进度和耗时自然排布；状态不靠彩色标签堆砌，长名称不挤压或溢出。
- 运行中只让状态点、边缘进度或当前连线做细小连续动画，不让整张卡片呼吸/跳动。状态切换、选中、右栏展开和定位必须自然连贯丝滑。
- 连线区分已走、当前、候选、未走和异常，优化线宽、转角、箭头、标签与间距；删除密集虚线和廉价发光。
- 当前节点变化时保留缩放，平滑移动到扣除右栏后的舒适位置；用户手动移动后停止抢视口，保留“回到当前节点”。
- 失败节点必须一眼可见，点击后右栏先说清发生了什么、为什么、用户能做什么，再显示动作、阶段、分类、时间、请求/响应摘要、trace、脱敏 cURL、日志位置和本次运行冻结的路径/人员/参数/字段权限/模式/断点。
- 不显示实例 ID、节点键、英文状态等内部信息；复制成功和失败都要有反馈，缺少证据要明确说明缺哪项。
- 用真实运行数据检查浅色、深色、侧栏展开/收起、常用桌面宽度和长节点名；保留自然动画，不新增减少动画约束。

### 7. 重建有效测试，再做十个真实样本

- 扩充 `test/run-f019.sh`，真实执行 F-019 新测试；不能只 grep 端点常量或复跑 F-016/F-017 旧用例。
- 为 15 个动作建立表驱动测试，至少贯穿：门禁→人员解析→会话→实时任务→载荷→端点→响应分类→事实重读→落账。覆盖缺字段、多人不唯一、权限变化、无待办、业务拒绝、鉴权拒绝、响应丢失和不变类动作。
- 测试响应形状必须来自参考后端和已脱敏真实响应，不自创协议。关键负例要先证明旧代码会失败，再证明修复有效。
- 先完整跑通 1 个 FormMaking 和 1 个 Vue 自定义表单，修掉共性问题后，再完成另外 6 个 FormMaking 和 2 个 Vue 自定义表单。无权限或流程配置不合适就换候选，不能把失败样本计入十个有效样本。
- 每个样本必须从创建计划、配置数据/人员/动作开始，至少完成一次成功运行，并从运行列表→执行路径二级页→运行面板核对真实目标事实。
- 十个样本合计真实覆盖单步、自动、人工控制、自动↔单步、安全边界、暂停/继续/停止、首次写/节点/动作断点、串并行、并发限制、动作次数循环、刷新与服务重启、失败诊断和整次运行删除。
- 如果流程缺审批人，应先选择一个已配置审批人的可操作流程；不能拿不合适的模板反复失败后归为环境阻断。

## 审查规则

每完成上面一个阶段并提交后，用 CC/Claude 审查该阶段的 diff、真值表和验证证据，修复意见后复测并再提交。CC 若报余额不足或找不到模型，立即跳过；首次不可用时只创建一个 **GLM-5.3-Flash、最高推理** 的专用审查线程，后续复用。CC 其他阻断最多重试三次，再切到该线程；后备也不可用则自行逐文件审查并继续，不停等。

审查必须特别查：是否仍有空人员、混用身份、不可达动作、写后盲重试、通用响应误判、未执行业务钩子、内部 ID 泄露、只改样式没补数据，以及测试没有走真实执行链。

## 完成标准

以下全部满足才能说“完成”：

- 上述 11 个已确认缺口全部关闭，旧报告状态已纠正；
- 动作删除/排序/次数/人员保存后刷新一致；
- 15 个动作都有源码真值、完整执行链测试和动作级证据；未验证动作不能登记为可运行；
- 保存草稿→提交/重提→审批/恢复可以在同一主实例上正确推进，人员切换和任务身份准确；
- 业务生命周期处理与目标页面顺序一致，未知处理明确阻塞；
- 会话策略不会互踢或在不确定时重发，流程控制不多走、不漏走、不重复写；
- 节点卡片、连线、动画、定位、点击反馈和右栏达到可交付质感，信息都来自同一次真实快照；
- 7 个 FormMaking 和 3 个 Vue 自定义表单各有一次完整成功运行，覆盖矩阵全部有可追溯证据；
- 受影响测试、前端类型检查和构建通过，浏览器无未处理错误；
- 最终生成 `docs/auto/2026-09-07-delivery-repair-report.md`，逐项列源码依据、修复提交、测试、真实运行和截图。任何一项未满足就写“未完成”，不得再写“已收口”。
