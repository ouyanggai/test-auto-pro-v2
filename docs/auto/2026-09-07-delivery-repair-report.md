# 交付缺口返工报告（截至 2026-09-07 08:55 窗口）

- 任务书：`docs/auto/2026-09-07-delivery-repair-task.md`。
- **总体状态：未完成。** 11 个已确认缺口中本窗口关闭 3 个（缺口 1、7、8 的点名违规项），真值表（阶段 1）完成并有重大纠偏发现；其余缺口如实列为未完成，不写「已收口」。
- 本窗口双线程并行：本线程负责配置保存、真值表、画布与测试脚本；并行线程负责会话与提交传参（`e4a1841`、`d58cfb8`、`93e08f2`，见验收报告附录「用户指路后的两项系统性修复」）。2026-09-07 加签返工已按目标 `updateFlowProxy` 整树协议接入，待真实写验证。

## 一、已完成项（每项：依据→修复→测试→验证）

### 缺口 1：旧验收报告状态纠正 —— 已完成（`1b67c68`）

- 依据：F-019 文档状态记录保留六项未修复结构性缺口，与原报告「已收口」直接冲突。
- 修复：报告头部改为状态纠正说明，指向返工任务书；保留仍然有效的样本推进与会话取证部分。

### 缺口 7：动作配置删除保存未生效 —— 已完成根因收口（`8f795c6`）

- **真实复现**（返工任务书第 2 步要求）：对运行中服务真实 API 实测——计划 21/路径 2156 删除「暂存当前表单」→ PUT 保存 → GET 重读一致；恢复原文 → 重读一致。**证明后端全链路（HTTP→服务合并→MySQL→投影）没有缺口**，同时排除了「服务端重编译覆盖」假设。
- **根因（前端）**：`saveCurrentNode` 在节点保存后 `reloadConfiguration()` 按服务端权威响应重建整份草稿，用户在节点编辑器里对实例级动作的删除/新增先于重读被清空，`instanceDraftHasUnsavedChanges` 从此恒为 false，实例容器保存（`saveInstanceActions`）成为死代码；刷新后动作复原，呈现为「删除→保存→重新进入仍存在」。
- 修复：节点保存与全量保存都在重读前留存实例草稿、重读后还原再保存实例容器；实例保存失败时还原用户编辑，失败不丢稿。
- 测试：服务层新增「删除→保存→重读一致」「清空保存」回归用例（`TestSaveActionConfigurationDeleteRemovesStoredActions`）；`vue-tsc`、`go test` 通过。
- **待人工复验**：真实页面（浏览器）删除实例级动作→保存→刷新不再复原，由用户手工核对。

### 阶段 1：目标动作真值表 —— 已完成（`a86fa1f`，`docs/auto/2026-09-07-target-action-matrix.md`）

- 15 个动作全部从按钮处理函数追到 api/index.js → web 网关 → workflow-center-api → workflow-center 服务实现，每条事实带 `文件:行号`；前置钩子顺序（`beforeSubmitAndDraft`/`beforeSubmitAndDraftNoBiz` 及 temporary/reInit 参数差异）、响应形状、写后重读逐项写清。
- **重大纠偏发现：加签端点错误**——目标平台加签走 `updateFlowProxy` + 整棵流程代理树 + 所有权复制（`FlowInstanceServiceImpl:1175-1221`）。工具已完成返工：使用完整代理文档并在写前追加节点人员，真实写仍需人工验证。
- 另两项直接影响执行链实施的事实：移交/回退/取回都更换批次并使原 jobTaskId 失效（写后必须重读 `/web/flowJobTaskLink/list` 重建任务映射）；暂存表单可用 `queryStorageFormData` 回读作为「检查点已更新」的确定判据。
- 评审说明：CC/Claude 评审通道本会话延续 403 不可用（沿用验收报告记录），按任务书规则改为逐文件自查；真值表由三个只读探查线程交叉取证后汇总裁决。

### 缺口 10（首步）：run-f019.sh 纳入真实动作编排回归 —— 部分完成（`e0266d4`）

- 测试范围补入 `action_orchestration` 包与动作/人员投影用例，必需用例清单加入删除保存回归；脚本 `bash -n` 与用例选择器实测通过。
- **未完成**：15 个动作的「门禁→人员→会话→实时任务→载荷→端点→响应→重读→落账」表驱动全链路测试仍缺（依赖缺口 2/3/4/5 的执行链修复）。

### 缺口 9（点名项）：右栏复制反馈与内部 ID —— 部分完成（`05116c8`）

- 复制日志位置/可重放 curl 的成功与失败都有明确消息提示，剪贴板不可用说明原因与替代路径（原为静默吞错）；空 curl 记录也提示而不是无响应。
- 最终目标事实不再渲染主实例内部 ID（用户裁决内部标识不上界面），保留状态/节点/待办等可读事实。
- **未完成**：错误详情的完整冻结配置（路径/人员/参数/字段权限/模式/断点）与缺失证据的明确说明、错误面板「发生了什么/为什么/能做什么」信息层级。

### 缺口 8（点名违规项）：整卡脉冲删除 + 节点卡片步骤事实 —— 部分完成（`c75bf8e`）

- 删除 `flow-node-run-pulse` 整卡呼吸动画（任务书明确点名的违规项），连续反馈由状态点呼吸与旋转指示器承担。
- 节点卡片新增已落账步骤事实行（第 N 步 动作 · 处理人 · 耗时），数据取自运行详情 DTO `steps` 同源事实，`vue-tsc` 与生产构建通过。
- **未完成**：连线已走/当前/候选/未走/异常的全面区分与质感重做、当前节点平滑定位与视口接管细节、右栏失败诊断完整改造（缺口 9 全部）。

## 二、未完成项（不虚报）

1. **缺口 2**（gate 无 save_draft/resubmit 可达分支；任务动作发送前实时补齐 jobTaskId）——未修复。resubmit 载荷分支已在适配层，执行链分派不可达（真值表第 3 节）。
2. **缺口 3**（`person_strategies` 冻结进运行上下文、按策略解析执行人、人员不唯一明确阻塞）——未修复；并行线程已修提交侧 `nextAuditorList`（`e4a1841`），人员策略接入执行链仍缺。
3. **缺口 4**（移交/加签空 `userIds`、`batchNo` 未承接、转发 receiverId 实时候选）——移交的 `batchNo/userIds` 已接入实时任务快照；加签已改为不发送 `userIds`，改发目标要求的完整代理树；转发 receiverId 实时候选仍待后续真实验证。
4. **缺口 5**（实例 ID / 流程代理 ID / 任务身份混用）——预填链已实现（前窗 `a5f325c`），提交载荷改用代理 ID 仍缺。
5. **缺口 6**（formTemplate 删除 `beforeSubmitAndDraft`/`beforeSubmit`；生命周期白名单适配）——未修复。
6. **缺口 9 其余项**（错误详情缺完整冻结配置与缺失证据说明）——复制反馈与内部 ID 两处点名项已修，其余未完成。
7. **缺口 10 主体**（15 动作表驱动全链路测试）——未完成（见上）。
8. **缺口 11**（会话限制归因）——并行线程已按「先证明原因再改策略」落地：写前强制重登改为缓存复用+失效恢复（`d58cfb8`），「账号级会话限制不是工具缺陷」的旧结论已在纠正后的报告中降级为待复核。
9. **§7 十个真实样本**——未开始；需在会话/传参线程收口后用骆蒙恩账号（或按目标行为换号）从创建计划跑到终态并留运行证据。
10. 全量脚本复跑——本窗口收口前实测 `test/run-f019.sh`：全部用例 PASS，唯一例外 `TestF018DimensionReadsAgainstRealTarget` 按既有跳过路径跳过（「已发列表前几条都没有动作痕迹，请待账号有已完结实例后重跑」），属骆蒙恩账号当前真实数据状态（该账号会话/数据正被并行线程高频使用），与本窗口改动无关；本窗口新增用例（删除保存回归等）全部 PASS。其余 7 个定向脚本与真实页面人工复验（删除保存修复、节点卡片视觉）仍待补做/由用户浏览器核对。

## 三、本窗口提交清单（全部中文说明、仅本地提交）

`8f795c6` 删除保存根因收口 → `1b67c68` 报告状态纠正 → `a86fa1f` 目标动作真值表 → `e0266d4` run-f019.sh 回归扩充 → `c75bf8e` 画布节点卡片事实行+删整卡脉冲。
