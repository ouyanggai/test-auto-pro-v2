# F-018 对账与安全重试

- 状态：ready_for_manual（用户 2026-09-05 裁决：后续切片连续实施，人工验收统一后置）
- 产品依据：`docs/PRODUCT.md` 产品原则第 6 条（写操作结果不明确时先核对真实状态，禁止盲目重发）、第 2 条（工具问题与目标平台问题必须分开说明，暂时无法判断时按工具问题处理）、第 4 条（产生运行记录后固化），以及「明确不做」中的「跳过失败写动作」
- 架构依据：`docs/ARCHITECTURE.md` 的「系统边界」（`internal/engine` 承载执行状态机，目标请求只能由 `internal/adapter/target` 发出）
- 纲领依据：`docs/EXECUTION_PROGRAM.md` 第 4.1 节 `internal/engine/reconcile` 包边界（不自动重发写请求）、第 4.4 节写结果不确定与安全重试、第 6.2 节 `recovery.log`、第 6.4 节 `error_class`、第 7.1 与 7.2 节 `run_step_attempts` 的对账结论/恢复动作/是否重放三列、第 7.4 节失败分类、第 9 节 F-018 行、第 10 节写端点白名单纪律
- 语义依据：`docs/TARGET_SEMANTICS.md` 第 1 节错误语义（三值判定规则、前置拒绝清单、乐观锁提示）、第 2 节幂等与重复提交（2.1 目标平台没有幂等键、2.2 `batchCode` 禁令、2.3 两道可能拦住重复写的防线、2.4 跨存储非原子导致的部分生效、2.5 待实测结论与重复写探针清单）
- 计划确认时间：2026-09-05（本次按用户要求补齐计划）
- 前置条件：F-016（`待对账`状态、`verdict` 三值判定、运行记录六张表、七阶段）与 F-017（运行控制、`control.log`、暂停原因区）必须已实施完成并由用户明确验收。本切片是 F-016 留下的那个缺口的唯一出路。

## 目标

让停在`待对账`的路径运行有且只有一条合法出路，而这条出路由只读对账的结论决定，不由用户猜、也不由工具赌。

## 为什么这是最危险的一个切片

目标平台没有幂等键（`docs/TARGET_SEMANTICS.md` 第 2.1 节，源码可证），一次盲目重发就是目标平台上一条真实的双写数据。F-016 因此把重发这条路彻底堵死，代价是不确定写只能停在`待对账`不动。本切片要把这条路重新打开一条缝，缝的宽度必须由证据决定：

- 只有当重读事实**在每一个维度上都证明写没有发生**时才允许重放。任一维度读不到、读不全、互相矛盾，都归为仍无法判定，不重放。
- 跨存储非原子的部分生效（第 2.4 节：审批端点在乐观锁检查之前已写入 Mongo 表单数据且不受该事务约束）必须被识别为独立形状，且映射到不重放。重放会把表单数据再写一次。
- 对账过程本身只允许读。`internal/engine/reconcile` 包不得引用任何写端点，这一条用测试锁定，不靠自觉。

## 单一用户结果

一条路径运行因写结果不确定停在`待对账`。工具自动做一次只读对账，然后在界面上给出一句中文结论、逐条判断依据（实例状态、当前节点、当前待办、已办记录、动作痕迹各自读到了什么）、以及**唯一一个**可执行的后续动作：

- 判定上一次写已生效：唯一动作是「确认并前进到下一步」。
- 判定上一次写未生效：唯一动作是「重放这一步」。重放是一次新的尝试，重新走完整七阶段，包含门禁复验。
- 仍无法判定：不提供任何前进或重放入口，只提供「记录人工核对结论并结束本路径运行」，让用户把他在目标平台上亲眼看到的事实登记为人工结论事实。

用户在任何时候都不会看到「重试」和「继续」两个按钮让他二选一。对账依据、结论和最终决定全部写 `recovery.log` 并落 `run_step_attempts`，可在 code-server 里逐行核对。

## 范围

### 包含

- `internal/engine/reconcile` 包：纯只读对账。输入是上一次尝试的写意图与响应事实、以及重读到的目标事实；输出是三值结论、唯一合法后续动作、逐维度依据清单与中文说明。
- 强证据规则：判定"未生效"必须五个维度同时成立（实例状态未变、当前节点未变、当前待办仍是原待办、已办记录无该动作痕迹、动作痕迹无本次写的迹象）。任一维度缺失或矛盾即降级为仍无法判定。
- 部分生效形状：表单数据已变但流程未推进（第 2.4 节）作为独立识别结果，结论固定为仍无法判定，不允许重放，并在界面明说"表单数据可能已经写进去了"。
- 重放的安全边界：重放前必须重新对账（不能沿用旧结论）；重放作为新的一次尝试记录（`attempt` 递增）；重放必须重新通过 `gate`；一次尝试仍然最多一次写请求；重放次数上限走配置（默认 1 次），超限固定进入仍无法判定。
- 自动对账与对账失败：进入`待对账`后执行器自动做一次对账（只读，安全）；对账因目标抖动失败时按 F-016 的有界重试与退避处理，仍失败则如实说明"暂时无法对账"，保持`待对账`并提供「重新对账」入口（只读，可以给）。
- 崩溃恢复的衔接：F-016 把重启后处于`运行中`与`核验中`的路径运行一律置为`待对账`。本切片让这批路径运行第一次真正有出路，走同一套对账逻辑，不为崩溃单独开一条路径。
- `recovery.log` 写入器（纲领第 6.2 节，F-013 已把它归属本切片）：对账输入、逐维度判断、结论、依据、最终决定、是否重放逐行可查，字段语义沿用 V1 已验证形态，行格式与关联键与 `step.log`、`control.log` 一致。
- `run_step_attempts` 的对账结论、恢复动作、是否重放三列真实写入（列由 F-016 建），以及对应的 `run_events` 中文标签事件。
- 人工结论事实：用户登记他在目标平台上核对到的实例状态与当前节点，作为 append-only 事实落库，路径运行随之进入终态。人工结论不覆盖任何机器判定，两者并存可回放。
- 失败分类对齐（纲领第 7.4 与 6.4 节）：对账后确定失败的路径运行按`目标拒绝`落分类；工具侧对账缺陷按`工具缺陷`落分类，同一件事在界面与日志里同名。
- 界面：`待对账`区域给一句结论、逐维度依据、唯一动作按钮、日志入口。落在 F-016 与 F-017 已有的运行画布与侧栏上，不新建页面。

### 不包含

- 写端点白名单扩张。仍只有 `/web/flowInstanceApi/submit` 与 `/flowInstanceApi/audit`；重放用的是原步骤自己的端点，不新增端点。
- 动作全集、恢复步骤与导航步骤的真实执行。属 F-019。
- 多路径调度与定时启动。属 F-020。一次运行仍只跑一条路径。
- 运行记录分析视图与事件流时间线。属 F-021。
- 自动重发、后台静默重试、失败自动跳过。前两条与产品原则第 6 条冲突，第三条产品明确不做。
- 修改 F-014 的三值判定规则与 F-016 的七阶段判据。本切片只在`待对账`之后接一段只读判断。
- 让对账去写目标平台，包括"用一次只读探针试探"以外的任何请求。

## 设计要点

### 三结论三动作，一一对应

| 对账结论 | 唯一合法后续动作 | 路径运行状态 |
| --- | --- | --- |
| 已生效 | 确认并前进到下一步 | 回到`运行中`，步骤游标前进 |
| 未生效 | 重放这一步（新尝试，重走七阶段） | 回到`运行中`，步骤游标不动 |
| 仍无法判定 | 只能登记人工核对结论并结束 | 保持`待对账`，登记后进入终态 |

一个结论只对应一个动作，这是纲领第 4.4 节的原话，也是本切片界面设计的唯一约束。界面不得在同一时刻渲染两个前进类按钮。

### 为什么"仍无法判定"给的是登记而不是选择

纲领要求这种情况"要求人工确认，不给第二个按钮"。本切片把人工确认落地为**登记用户看到的事实**，而不是让用户替工具选"算成功还是算失败"：

- 用户填写他在目标平台上看到的实例状态与当前节点，工具把它作为人工结论事实 INSERT 落库，并结束本路径运行。
- 要继续跑就新建一次运行（计划已固化，产品原则第 4 条）。这样工具永远不会在没有证据的情况下继续对同一个真实实例发写请求。
- 这一条是取舍，写进「未决与需要用户裁决的事项」。如果用户希望登记"已生效"后能继续跑完剩下的步骤，改动不大，但必须接受"继续的依据是人的口述而不是机器证据"。

### 强证据规则

判"未生效"是本切片唯一会导致再次真实写的分支，所以它的门槛最高：

| 维度 | 判"未生效"要求 |
| --- | --- |
| 实例状态 | 读到且与写之前一致 |
| 当前节点 | 读到且与写之前一致 |
| 当前待办 | 读到且仍是写之前那一条 |
| 已办记录 | 读到且不含本次动作 |
| 动作痕迹 | 读到且无本次写的迹象 |

五个维度全部成立才判未生效。任一维度读不到、超时、为空或与其它维度矛盾，一律降级为仍无法判定。降级理由逐条写进 `recovery.log`，不允许只写一句"无法判定"。

### 部分生效必须单独识别

`docs/TARGET_SEMANTICS.md` 第 2.4 节已勘定：审批端点在乐观锁检查之前就把表单数据写进了 Mongo，且不受该事务约束。因此存在"表单数据已变、流程未推进"的真实形状。

- 对账识别到这个形状时，结论固定为仍无法判定，绝不判未生效。
- 界面必须明说"表单数据可能已经写进去了，重放会再写一次"，让用户理解为什么工具不给重放。
- 这条规则由对照测试锁定，不允许后续改动把它优化掉。

### 重放不是重发

重放是一次新的尝试，走的是完整流程，不是把上一次的请求再发一遍：

- 重新对账 -> 重新 `plan` -> 重新 `gate`（门禁此刻不通过就停止）-> 重新 `prepare`（重新解析演员与会话）-> `submit` 一次 -> `verify` -> `settle`。
- `attempt` 递增，新的 `trace_id` 与 `curl_trace_id`，`run_step_attempts` 增一行，`is_replay` 为真。
- 重放次数上限走配置，默认 1。用完仍不确定就固定停在仍无法判定，不给第二次。
- 重放全过程受 F-017 的断点与控制约束：单步模式下重放同样需要用户放行。

### 对账只读，用测试锁定

- `internal/engine/reconcile` 不得导入任何发写请求的入口；契约测试断言该包的依赖集合里没有写端点常量与写方法。
- 对账过程中发出的目标请求全部是读，动态录制断言实际请求集合与写端点白名单交集为空。
- 这一条与 F-012 建立、F-016 改造为白名单的零写入断言体系是同一套机制，不另造。

### 日志与记录

- `recovery.log` 与 `step.log`、`control.log` 同目录同格式，关联键至少含 `run_id`、`path_run_id`、`step_id`、`attempt`、`phase`、`trace_id`。
- 对账的每一维度判断各一行，结论一行，最终决定一行。事后要能只看这个文件复盘"为什么当时不给重放"。
- 对账结论与状态变化同事务落库，事件行同事务追加（纲领第 7.1 节）。
- 迁移：`run_step_attempts` 的对账三列由 F-016 建立，正常情况下本切片不需要迁移。确需补列时取编号 `028`，不复用编号，并在状态记录写明补了什么。

## 接口与记录约束

- 对账输入只接受上一次尝试已经落库的写意图、传输阶段事实、响应初判和目标只读事实；函数不得接受目标写客户端、凭证或浏览器提交的“继续/重试”选择。
- 领域结论固定为 `effective`（已生效）、`not_effective`（未生效）、`indeterminate`（仍无法判定）。每个结论必须带五个维度的证据状态、中文依据和可执行动作；缺失字段不得被默认值填成“未生效”。
- 合法恢复动作固定为 `advance`、`replay`、`manual_end`、`reconcile_again`。其中 `replay` 只能由 `not_effective` 产生，`reconcile_again` 只在对账读取失败时出现，其他组合一律拒绝。
- 重放接口必须带当前 `path_run_id`、`step_id`、上一次 `attempt` 和幂等键。服务端在同一事务内重新核对路径状态、重放次数上限和当前租约，重复请求只能返回第一次决定，不能产生第二条写尝试。
- `run_step_attempts`、`run_events`、人工结论事实只追加不覆盖；聚合状态变化与事件追加在同一事务完成。若需要新增人工结论表，迁移编号使用 `028`，不得复用已有编号。
- 对账读取不得调用 `/web/flowInstanceApi/submit`、`/flowInstanceApi/audit` 或动作目录中任何写操作；目标请求白名单契约必须断言对账请求集合与写端点集合交集为空。

## 口径对齐（2026-09-05 核查，多线程实施前必读）

本节由核查线程统一写入 F-017 至 F-023 七个功能文档，内容完全一致，避免各线程口径不一。
与本节冲突的本文件其余表述一律以本节为准。

1. **界面与交互按纲领第 12 节执行。** 该节是跨切片基线（含六条硬性禁止：裸 input、操作堆砌、整页遮罩、
   空白区域、界面暴露内部标识、陈旧文案）。功能文档只写本切片新增的交互，不重复也不弱化基线。
2. **写载荷口径已被 F-024 改写。** 任何携带表单数据的写请求，正文必须是
   「目标实例当前完整表单数据 + 本节点声明可编辑且已配置的字段」，构造入口是
   `internal/engine/step.BuildNodeFormData`；发起没有实例时基线是发起态完整表单模型，
   只去掉「只有后续节点才能编辑」的字段。禁止直接透传 `runCtx.EffectiveFormData`
   （目标保存表单数据是整份覆盖，见语义清单第 16 条）。
3. **节点标识口径。** 编译场景与配置快照里的 `nodeKey` 是哈希派生的不透明键，不能发给目标。
   凡是与目标对照（待办读取、按节点写参数、事实重读、对账）都必须用 `NodeInfo.TargetNodeID` /
   `StepPreview.TargetNodeID`；任务级动作拿不到真实标识时必须阻塞，不得用空值比对。
4. **迁移编号。** 现有最大编号为 `029`（F-018 的尝试写前事实）。新增迁移从 `030` 起，编号一律不复用。
5. **语义清单现状。** `docs/TARGET_SEMANTICS.md` 已有 19 条（第 15 条手动分支提交传参、
   第 16 条表单数据整份覆盖、第 17 条条件求值数据来源、第 18 条对账两个只读维度、
   第 19 条已发列表的业务关联过滤会排除本工具发起的实例），证据块共 34 个，
   漂移检测脚本 `test/contracts/f014/semantics_evidence_drift.sh` 已在用：新增语义一律扩展它，不另造。
6. **动作与写端点的准确数字。** `internal/model` 有 16 个 `ActionKey`；写端点共 11 个
   （`write.go` 2 个 + `write_actions.go` 9 个）。任何计划里的动作数、端点数以此为准。
7. **控制端点当前按运行 ID 寻址。** `RunOrchestrationService.resolvePathRunID` 经
   `GetPathRunByRun`（SQL 为 `ORDER BY id LIMIT 1`）解析，全部 8 个控制端点共用。
   这是一次运行只跑一条路径的前提；F-020 引入多路径时必须先改成按路径运行寻址。
8. **既有实现必须先读后写。** 实施前先用 `git log --oneline` 与目录清单确认本切片是否已有落库实现，
   已有实现按「补齐与返工」推进，不重建第二套；重复实现属于交付缺陷。

### 评审修复与界面返工（2026-09-05 核查线程，已提交）

**对账原先永远只能得出「仍无法判定」——已生效与未生效都是死代码。**
`Collect` 里已办记录维度只在 `DoneRecordsRead` 为真时才有证据，动作痕迹维度只在 `ActionTraceFound`
为真时才算变化、否则一律标 `missing`，而收集器（`internal/engine/control/reconcile.go`）两个字段都没填，
于是五维必然缺失、强证据规则必然降级：`ActionAdvance`（确认前进）与 `ActionReplay`（重放）从未可达，
每次对账都只剩「登记人工核对结论」。单元用例直接喂 `Dims`，把这个缺口完全遮住。

修复方式是把两个维度真的读起来，而不是放宽判据：

- 新增 `Client.FindDoneTaskOnNode`：已办与待办同表同端点（`/web/flowJobTaskLink/list`），
  只是 `taskStatus` 取 `done`（`TaskStatusEnum.done`=已办），按实例与节点精确匹配。
- 新增 `Client.FindAuditTraceOnNode`：读 `/web/flowAuditRecord/list`（与目标自己的流程日志同源，
  请求只带 `flowInstanceId`），按 `flowNodeProxyId` 判断本节点是否已留下动作痕迹，并回报该实例的记录条数进依据说明。
- 两个读取都走只读有界重试；读不到时如实标缺失并写 `step.log`，绝不把「没读到」当成「没有痕迹」。
- 判定器区分 `ActionTraceRead`（读到了但没有痕迹→未变化）与读不到（→缺失降级）。
  「未生效」仍然要求五维全部读到且全部未变——它是唯一会导致重放（再写一次）的结论，这条不放宽。
- 新增 3 个用例锁定：五维齐备才允许重放、任一维度读不到即降级且不给重放、读取失败只能重新对账。

**人工结论登记从四个裸 `<input>` 改为真正的表单**：实例状态改为目标真实状态集合的下拉
（含「目标平台上看不到这条实例」），当前节点与登记人为必填文本，补充说明为多行选填；
三项必填带中文校验文案与失焦校验，提交前先过 `validate()`，再经 `NPopconfirm` 二次确认
（说明登记后路径运行进入终态、不能再放行或重放）。顶部补一句中文引导，说明这是在登记「你亲眼看到的事实」。
`test/contracts/f018/reconcile_readonly.sh` 增加反向断言：不得裸 `input`/`select`、必须有校验与二次确认、
两个新维度必须有真实只读实现并被收集器填入。

### 真实目标实测（2026-09-05，用户指定账号，全程只读）

两个新增维度已用真实账号跑通，不再是源码推断：

- 已办记录：`/web/flowJobTaskLink/list` + `taskStatus=done` 实测 HTTP 200、`isSuccess=true`，
  返回记录带 `flowInstanceId` 与 `flowNodeProxyId`（同端点 `waiting_send`／`has_been_sent` 形状一致）。
  从该账号自己的已办列表取一条记录回读必然命中（实例 `03b9a7a6…` 节点 `4b3b3591…`）。
- 动作痕迹：`/web/flowAuditRecord/list`（请求只带 `flowInstanceId`）实测返回数组，
  元素带 `flowInstanceId`、`flowNodeProxyId`、`auditStatus`；用响应里真实出现的节点标识回读必然命中
  （实例 `312c3f31…` 节点 `993daf28…`，该实例 1 条记录）。
- 节点过滤真的生效：两个维度传不存在的节点标识都返回未命中而不是报错；空实例标识不发请求。

三个真实只读用例（`test/integration/f018_dimension_reads_test.go`）已纳入 `test/run-f018.sh`，
逐条核对通过记录、禁止跳过。结论同步为 `docs/TARGET_SEMANTICS.md` 第 18 条并通过证据漂移检测。

### 真实不确定写与恢复层收口（2026-09-05，用户授权，账号骆蒙恩）

用户明确授权做一次真实的不确定写，要求当场收口、不把问题推给下个任务。实测把三件事查清了。

**第一件：恢复层在真实产品里原本整段不可达（死代码），单元用例喂 `Dims` 把它完全遮住了。**
四处缺陷各自都足以让三个恢复动作一个都执行不了：

1. `approveOneStep` 在判定非「确定成功」时一律 `s.clear(pathRunID)` 删掉内存现场。
   不确定写落账后现场就没了，`POST /api/runs/{id}/reconcile` 只会返回「当前没有等待放行的步骤」——
   纲领要求的「进入待对账后自动对账一次」在真实运行里从来没有发生过。
2. 待对账被当成不可离开的终态，`RecoveryAction` 第一件事就是 `IsTerminalPathRunStatus` 拒绝，
   三个动作全部返回「运行已结束」。
3. 迁移表 `pathRunTransitions[待对账] = {}`，租约领取的 SQL 也把待对账排除在外，
   重放没有回到运行中的路，即使绕过第 2 条也会在阶段 4 领取推进权时被状态冲突拒绝。
4. `ListRunAttempts` 没取 `id`，`LatestStepAttempt` 返回的尝试主键恒为 0，
   对账三列的 `UPDATE ... WHERE id = 0` 命中 0 行且被静默忽略——对账跑过也不会留下记录。

配套的记账缺陷三处：重放上限（默认 1 次）因为每次对账都把「已用重放次数」重置为 0 而从未生效；
`is_replay` 永远写死 false；对账事件行的 `run_id` 填的是路径运行 ID。

修复后按纲领第 4.4 节的设计打通：待对账成为唯一的「可恢复停摆态」（`CanRecoverPathRunStatus`），
迁移表增加待对账 → 运行中这一条通路，不确定写保留内存现场，重放先回运行中再重建预览（重过门禁）
并以「库里最近一次尝试号 + 1」新建尝试；重放次数用完后服务端把唯一动作降级为「登记人工核对结论」，
界面不会再渲染一个点下去必然被拒的重放按钮。

**第二件：真实不确定写与对账首次真的跑起来了（运行 12）。**

- 计划 11（oyg00，账号骆蒙恩）路径 1121，单步模式，放行第 1 步「发起」。
- 目标受理（`isSuccess=true`，实例 `6bd617f3069d462d8bfe63ba12b35739`），
  核验重读未见实例 → 「成功声明 + 明确未变」→ 不确定 → 待对账。trace `c6ea7555b51afaee`，写请求耗时 464ms。
- 只读对账**第一次在真实目标上执行成功**，五维真实读数：三个由实例事实派生的维度读不到、
  已办与动作痕迹按真实读取结果给出；结论「仍无法判定」，唯一动作「登记人工核对结论并结束」。
- 拿未生效动作（重放）去试探被服务端按中文原因拒绝：「当前唯一合法动作是 manual_end，不接受 replay」。
- 登记人工核对结论后：`run_manual_conclusions` 落一行，`run_step_attempts` 的对账三列
  首次真的写进去（`indeterminate` / `manual_end` / `is_replay=0`），`recovery.log` 首次有真实内容
  （逐维依据各一行 + 结论一行）。日志：`logs/plans/oyg00__plan-11/runs/路径 1__path-1121/12/`。

**第三件：这次不确定写的根因是工具自己的读取口径，不是目标没写成功。**

只读复查证明实例真实存在且已推进到「资源开发部」（处理人程鹏），审核记录有一条「流程发起」。
逐项试验定位到 `FindSubmittedFlow` 的查询里 `flowInstanceBizRelevanceList=[{otherBiz:company,otherBizId:""}]`
会把本工具发起的实例整条排除（本工具的发起载荷不带业务关联）。去掉这一个字段立刻命中。
结论落 `docs/TARGET_SEMANTICS.md` 第 19 条，并用两个真实只读用例 + 一条契约反向断言锁死。

修复后在同一条路径上再跑一次真实发起（运行 13，实例 `7bcf0c29f1054ba0bed5cf367ba2f2d2`，
trace `0715f0cdf7cc2700`）：判定为**确定成功**（「目标声明成功，且重读确认流程已按期望前进」），
路径正常前进到第 2 步，不再出现虚假的待对账。这同时清掉了 F-016 遗留的「实例可见性」未决事项。

### 「未生效 → 重放」拿不到真实目标证据的原因（结构性，不是少跑一次实验）

原计划把这条链路记为「一次真实的不确定写就能构造」。实测证明这个判断是错的，它当前结构性不可达，
三条原因各自独立、都有实测支撑：

1. **判「未生效」要求写之前实例已存在。** 五维里的「实例状态」在发起场景下只有两种可能：
   实例现在读到了（写之前不存在、现在存在 → 已变化），或现在读不到（→ 缺失降级）。
   两条都不可能是「与写之前一致」，所以发起类写永远得不到未生效。
   已固化为断言：`test/unit/backend/reconcile/f018_submit_not_effective_test.go`。
2. **对已有实例动手的动作在执行器里没有接线。** `PathRun.MainInstanceRef` 只在发起成功后回填，
   从计划的「待发/已发」来源拿不到；`BuildSubmitBody` 也没有 `data.id`，提交已有草稿同样发不出去。
   这属 F-019「恢复步骤的真实执行」，本切片范围明确排除。
3. **这个账号手上没有审批待办。** 实测扫全量 169 条待办：草稿 2 条、驳回 83 条、撤回 84 条，
   审批待办 0 条；历史审核记录显示该流程的审批节点历来由程鹏、陆澳、张宏伟、程晓云等人处理，
   骆蒙恩只做「流程发起」。而执行器一个计划只用一个账号会话（`PlanAccount`），
   因此用这一个账号拿不到「同意」的入口。

结论：这条链路的正确性由真实 MySQL + 真实状态机 + 真实仓储的集成用例保证
（`test/unit/backend/executor/f018_recovery_reachable_test.go`，只有目标读写是假件），
覆盖未生效 → 重放为新尝试（attempt 递增、`is_replay=true`、一次尝试只发一次写请求）、
重放上限 1 次真的生效、已生效只给确认前进、任一维度读不到即降级为人工登记。
真实目标证据要等 F-019 的恢复执行接线完成、且有一个手上确有审批待办的账号，才能取得——
这不是本切片漏跑的实验，而是它依赖的功能还没有实现。

### 本切片现状（核查线程如实记录）

后端与最小界面已落库（提交 `d8c11ad`）：`internal/engine/reconcile/reconcile.go`、
`internal/engine/control/reconcile.go`、`recoverylog.go`、迁移 `028_f018_reconciliation.sql`、
`test/run-f018.sh`、`test/contracts/f018/reconcile_readonly.sh` 均已存在。
因此本切片不是从零开始，按「补齐与返工」推进。

必须返工的具体项（核查已确认，均属纲领第 12 节禁止项）：

- `web/src/views/RunDetailView.vue` 的人工结论登记表单是四个裸 `<input>`（实例状态、当前节点、说明、登记人），
  只有 placeholder 没有中文标签、没有必填标记、没有失焦校验、没有提交前确认。
  这是对账链路上唯一的人工输入点，也是最需要防误填的地方，必须按纲领第 12.1、12.2 节重做。
- 唯一合法恢复动作的呈现没有与「不确定写」的判定依据、日志位置放在同一分区，用户要跨区域拼信息。
- 对账结论与恢复动作缺少不可逆提示：点下去会发真实写请求，按钮文案必须写清这一步会做什么。

## 详细执行任务

每个任务完成后独立验证并使用中文提交说明；未获批准前只维护本计划，不实现代码。

### T01：对账领域契约与纯判定器

在 `internal/engine/reconcile` 建立输入、五维证据、部分生效标记、三值结论、恢复动作和中文说明类型。实现纯函数：五项证据全部明确未变才得出 `not_effective`；任一缺失、超时、矛盾、部分生效或未知响应均得出 `indeterminate`；已生效只在五维读数与写意图一致且响应组合满足 F-014 判定时成立。纯函数不做 IO，不持有目标客户端，不改变运行状态。

### T02：只读事实收集与部分生效识别

在目标适配层增加对账所需的实例状态、当前节点、当前待办、已办记录、动作痕迹和表单变化读取聚合。每个维度保留“读到/为空/错误/冲突”事实；表单已变化但流程未推进时返回独立的 `partial_effect`，不得把它压成“明确未变”。对账请求走只读会话入口和 F-016 的有界退避，禁止复用写调用的重放入口。

### T03：对账服务与唯一恢复动作

实现进入`待对账`后的自动对账、重读失败后的「重新对账」以及三种结论对应的唯一动作。动作执行前再次读取并校验当前路径运行状态、租约、步骤游标和重放次数；`advance` 只推进一次游标，`replay` 创建递增 `attempt` 的新尝试并重新走七阶段，`manual_end` 只登记人工事实并结束路径。任何重复放行、过期版本或动作不匹配返回当前真实状态，不发写请求。

### T04：运行记录、人工结论与事务边界

把对账结论、恢复动作、是否重放、部分生效说明和失败分类写入 `run_step_attempts`，并追加对应中文 `run_events`。新增人工核对事实的最小模型（实例状态、当前节点、说明、登记人和时间）；人工事实与机器判定并存，不能更新或覆盖机器判定。对账结论、聚合状态和事件必须同事务提交，失败回滚后不留下半份恢复记录。

### T05：`recovery.log` 与双向日志引用

复用 F-013 运行目录和日志作用域新增 `recovery.log` 写入器。每个维度一行，结论、最终决定、是否重放和失败原因各一行；行中必须带 `run_id`、`path_run_id`、`step_id`、`attempt`、`phase`、`trace_id` 与日志行号。运行记录保存相对路径、行号和 trace 引用，日志反向带运行键，保证两边可互查。

### T06：恢复 API 与错误语义

新增只读对账触发、恢复动作和人工结论登记接口；接口响应只返回中文业务文案、不透明键、依据清单和当前可用的唯一动作。参数错误、路径状态过期、目标读取失败、工具存储失败和目标拒绝分别映射稳定错误码，统一通过现有失败响应与日志同源。任何接口不得提供并列的“继续”和“重试”按钮语义。

### T07：运行画布待对账工作区

在 F-016/F-017 已有运行画布与侧栏上增加`待对账`区域：显示一句结论、五维依据、部分生效警告、日志入口和唯一动作。`not_effective` 只显示“重放这一步”，`effective` 只显示“确认并前进到下一步”，`indeterminate` 只显示人工事实登记表；对账失败只显示“重新对账”。提交中不整页遮罩，动作完成后以服务端重读结果刷新。

### T08：测试、白名单与真实安全证据

新增 `test/unit/backend/reconcile/` 覆盖五维证据矩阵、缺失/冲突/部分生效和唯一动作约束；新增真实 MySQL 集成用例覆盖 append-only、事务回滚、人工事实和重放次数；新增目标只读录制用例与 `test/contracts/f018/`，断言对账无写端点、一次重放最多一次写请求、重放前必重新对账。真实写验证只使用用户指定的可污染账号和流程，账号值不进入仓库；汇总入口 `test/run-f018.sh` 禁止静默跳过。

### T09：语义证据与交接文档

把 F-018 实际覆盖的错误语义、部分生效和重放证据补入 `docs/TARGET_SEMANTICS.md`，记录参考仓库 HEAD、目标部署版本和测试引用；检查动作目录、运行状态机、日志字段和 API 文案是否存在同范围重复实现。完成自动验证后更新本文件、`docs/ROADMAP.md` 与 `docs/PROGRESS.md`，状态停在 `ready_for_manual`。

## 完成标准

- [ ] 界面与交互逐条对齐 `docs/EXECUTION_PROGRAM.md` 第 12 节基线：无裸 input／无操作堆砌／无整页遮罩／
      无空白态／界面不暴露内部标识／无陈旧文案；等待过程显示阶段与重试、读数同源且单调不减、
      不可逆操作有明确前置说明、自动跟随可接管、可键盘聚焦、状态不只靠颜色。
      定向验证入口含界面结构契约脚本反向断言，人工验收逐条给出可判断的交互步骤。
- [ ] `internal/engine/reconcile` 是纯只读判定包，依赖检查和动态录制均证明不触发任何目标写端点。
- [ ] 五维强证据规则、三值结论、部分生效降级和所有缺失/矛盾兜底均有单元用例；判“未生效”不存在任何放宽路径。
- [ ] 进入`待对账`后自动对账一次；目标抖动按配置预算有界重试，仍失败保持`待对账`并只提供“重新对账”。
- [ ] 已生效、未生效、仍无法判定分别只出现一个合法动作，不出现并列的继续/重试入口。
- [ ] 重放前再次对账，重放是递增 `attempt` 的新尝试，重新走七阶段、重新过门禁，一次尝试最多一次写请求；默认最多重放一次，超限不再提供重放。
- [ ] 表单部分生效被独立识别，结论固定为仍无法判定，界面明确说明“表单数据可能已经写进去了，重放会再写一次”。
- [ ] 人工结论事实 append-only 落库并使路径进入终态，机器判定、人工事实和事件可共同回放。
- [ ] `run_step_attempts` 三列、`run_events` 中文标签、失败分类和 `recovery.log` 逐维度记录齐全，日志与记录双向可达。
- [ ] 事务失败不会留下状态、事件或恢复动作的半份写入；重复恢复请求幂等且不会额外发目标写请求。
- [ ] F-014 的目标拒绝/工具缺陷分类在界面、记录和日志中使用同名中文；不修改 F-014 三值判定与 F-016 七阶段判据。
- [ ] 运行画布待对账区域、唯一动作、依据清单和日志入口可在现有页面完成，不新建运行详情页。
- [ ] `test/run-f018.sh` 实际跑通，当前范围单测、真实 MySQL 集成、只读录制、契约和前端构建无跳过；真实写证据按账号与流程单独记录。
- [ ] 未扩张写端点白名单；对账阶段实际请求集合与白名单交集为空。
- [ ] 已检查同范围相似问题（旧重试入口、`DoRead` 写路径重放、状态 UPDATE 覆盖事实、重复动作按钮），结论写入状态记录。
- [ ] 文档状态更新为 `ready_for_manual`，路线图和进度同步，并列出用户手工核对步骤。

## 未决与需要用户裁决的事项

本计划按以下默认方案编写，进入 `implementing` 前如需改变必须先在本文件记录裁决：

1. 仍无法判定时，人工登记只结束当前路径运行，不以人工口述结果继续使用同一个真实实例；如需继续，用户新建一次运行。
2. 重放上限默认 1 次，配置可调但不能超过系统安全上限；超限后固定进入人工结论流程。
3. 对账读取失败提供“重新对账”入口，但该入口始终只读，不允许把读取失败转换成重放。

## 状态记录

正常状态按 `preparing -> awaiting_approval -> implementing -> ready_for_manual -> accepted` 推进。

- 2026-09-05 `preparing` -> `awaiting_approval`：复核原计划时确认目标、范围和设计要点已有，但缺少详细执行任务、完成标准、状态记录、测试分类和人工验收；已按 `docs/PRODUCT.md`、`docs/ARCHITECTURE.md`、`docs/EXECUTION_PROGRAM.md` 第 4.1、4.2、4.3、4.4、6.2、7.1、7.2、7.4、9、10 节以及 F-014 至 F-017 计划补齐。本次只写计划，不进入实施。
- 2026-09-05 用户裁决：后续切片连续实施，人工验收统一后置；F-016/F-017 已实施完成，本切片进入 `implementing`。
- 2026-09-05 实施完成（T01-T08，停在 `ready_for_manual`）：`internal/engine/reconcile` 纯判定包（五维强证据、三值结论、部分生效固定降级、唯一动作映射，契约锁定零写端点）；对账事实收集器（实例状态/当前节点/待办三维已接入，已办记录与动作痕迹如实标注「未接入」按缺失降级——真实环境对账因此通常落在仍无法判定，这是强证据规则的诚实结果而非缺陷）；对账服务（待对账后自动只读对账、结论三列写回尝试行、唯一动作执行、重放为新尝试且上限 1 次、人工结论 append-only 表迁移 028）；recovery.log 写入器；恢复 API 与前端待对账工作区（一句结论+逐维依据+唯一动作，indeterminate 给人工登记表单）。
  如实记录：重放在当前实例可见性未解决前很难被真实触发（五维中已办/动作痕迹缺失即降级），其正确性由单元与集成用例保证；真实命中证据待实例可见性问题（F-016 记录）人工核对后补。
- 2026-09-05 用户要求根治「对账现场只在内存里」：写前基准随尝试行落库（迁移 `029`），
  新增 `control.Service.Rehydrate` 与应用层 `ensureReconcileSession` 按运行事实重建现场，
  基准缺失如实降级，待对账现场固定为「写结果不确定」不再产出放行命令，详情页进入待对账后自动对账一次。
  新增 3 个真实 MySQL 用例（重启后仍可对账并执行唯一动作、基准缺失诚实降级、待对账无放行入口）与契约断言；
  真实验证：运行 12 在服务重启后按运行事实重建现场并给出真实五维读数。
- 2026-09-05 用户授权真实不确定写并要求当场收口，本切片从 `ready_for_manual` 返回 `implementing` 修复后重新停在 `ready_for_manual`。修复项：恢复层四处不可达缺陷（不确定写后清空内存现场、待对账被按终态一律拒绝、迁移表与租约不允许回到运行中、尝试主键未取回导致对账三列写不进去）、三处记账缺陷（重放上限从未生效、`is_replay` 恒为 false、对账事件行的 `run_id` 填成路径运行 ID）、对账与恢复动作缺运行日志作用域、人工结论登记后运行聚合不收尾、收集器把两个独立维度的真实读数按「未接入」丢弃、按实例精确复查带业务关联过滤（F-016 实例可见性问题的根因）。真实实测：运行 12 取得真实不确定写与首次真实对账，运行 13 验证修复后同一条发起写判为确定成功。新增用例：恢复链路真实 MySQL 集成 4 个、结构性结论单测 2 个、实例可见性真实只读 2 个、契约反向断言 3 组，全部纳入 `test/run-f018.sh`。
- 门禁：F-016 与 F-017 必须先完成并经用户明确验收；本切片包含可再次触发真实写的重放，未获用户明确批准前不得进入 `implementing`。实施完成后停在 `ready_for_manual`，不得自动接受或开始 F-019。

## 人工验收

已有真实运行可直接核对（不需要再发真实写）：

1. 打开运行 12（计划 11 路径 1121）的详情页，确认待对账区域自动完成一次只读对账并给出一句中文结论、
   五维逐条依据、唯一动作按钮，并且没有并列的「重试 / 继续」两个入口，也没有「执行一步」。
   重启服务后再打开一次：现场会按运行事实重建，对账区域照样出得来（停摆原因会写明是重建的）。
2. 在同一页核对第 1 步的尝试事实：判定「不确定」、对账结论「仍无法判定」、恢复动作
   「登记人工核对结论并结束」、是否重放「否」，并能从这一行跳到 `step.log` 的具体行。
3. 打开 `logs/plans/oyg00__plan-11/runs/路径 1__path-1121/12/recovery.log`，
   逐行核对五维依据、结论与最终决定（含人工登记的实例状态、当前节点、登记人）。
4. 打开运行 13，确认同一条路径的发起写这次判为「确定成功」，并且路径按路径偏离规则停在第 2 步
   （停止原因写明「实际命中分支与已配置路径不一致」）。
5. 在目标平台上核对实例 `6bd617f3069d462d8bfe63ba12b35739` 与 `7bcf0c29f1054ba0bed5cf367ba2f2d2`
   确实存在、状态与工具记录一致——这是「工具问题与目标问题分开说明」的人工确认点。
6. 界面基线（纲领第 12 节）：人工结论登记表单三项必填带中文校验与失焦提示，提交前有二次确认并写明
   登记后路径运行进入终态；提交过程不整页遮罩。
7. 在项目根执行 `./test/run-f018.sh`，确认输出包含恢复链路四个用例、真实目标只读五个用例、
   对账契约与写端点白名单，全部通过且无跳过。

需要 F-019 之后才能人工核对的部分（本切片无法构造，原因见上一节）：

8. 「未生效 → 重放」的真实目标端到端核对：需要恢复执行接线完成，且用一个手上确有审批待办的账号。
9. 表单部分生效（表单已变、流程未推进）的真实事实：需要能对已有实例发出携带表单数据的写请求。

## 对账现场按运行事实重建（2026-09-05 用户要求根治，已完成）

原先的边界是：对账现场（本步预览、步骤游标、写之前的基准事实、已用重放次数）只存在于
`control.Service` 的进程内存里，服务一重启，停在待对账的路径运行就再也没有出路——
界面上待对账区域直接消失，只剩步骤事实与日志。历史上已有 10 条这样的运行（运行 1-10）。

根治的做法是把缺的那一块事实补进库，然后按事实重建现场，而不是让现场长期驻留内存：

- **写之前的目标事实随尝试行落库**（迁移 `029`，`run_step_attempts.before_facts`）。
  它是对账判定的另一半输入，与 `transport`/`initial`/`reread` 同类，因此归属尝试行；
  只在插入时写一次，永不 UPDATE。
- **`control.Service.Rehydrate` 按运行事实重建现场**：运行身份来自 `runs`/`path_runs`，
  步骤游标来自 `run_steps`（最近一步的最近一次尝试判「不确定」就指向它；否则说明是崩溃恢复，
  在飞的是下一步），已用重放次数按本步 `is_replay` 的尝试数还原，断点由控制事实回放，
  模式取自 `runs`，本步预览重新构建一次（plan 与 gate 用此刻的真实事实重算，只读）。
- **重建的触发点是对账与恢复动作**（`ensureReconcileSession`），不是服务启动：重建要登录目标做只读重读，
  按需触发才不会在启动时对一批历史运行成批发请求。执行上下文由应用服务层装配（它才读得到
  计划、路径配置、编译场景与节点真实标识），控制层只负责按运行事实补齐游标与基准。
- **基准缺失一律如实降级。** 尝试行早于迁移 `029`、或崩溃在落账之前时没有基准，
  此时由实例事实派生的三个维度全部标缺失并写明「写之前的目标事实基准没有落库，无从比较」，
  结论只能是仍无法判定。零值基准绝不允许被读成「写之前实例不存在」这个确定事实——
  那会凭空造出一条与事实相反的依据，还可能把已生效的写说成未生效。
- **保留现场不等于还能放行。** 待对账的现场（不论是执行中留下的还是重建出来的）控制状态固定为
  「写结果不确定」，可用命令是空集；界面上不会出现「执行一步」与唯一动作并列。

真实验证（2026-09-05）：运行 12 在服务重启后详情页的对账区域一度为空，调用对账接口即按运行事实
重建现场并给出真实五维读数（该运行的尝试行早于迁移 029，因此三个实例维度如实报「基准没有落库」，
而动作痕迹维度真实读到了本步节点的发起痕迹）。自动化覆盖见
`test/unit/backend/executor/f018_recovery_reachable_test.go` 的三个用例：重启后仍可对账并执行唯一动作、
基准缺失时诚实降级、待对账现场不提供放行入口。

## 已知边界（本切片如实记录，不藏在实现里）

- **一次运行只用一个账号会话**，因此需要不同处理人的多节点场景无法在一次运行里跑完（属 F-020/F-019 范围）。
- 迁移 `029` 之前落账的尝试行没有写前基准，这些历史运行重建后只能得到仍无法判定；
  这是诚实结果而非缺陷，界面与 `recovery.log` 都写明了原因。
