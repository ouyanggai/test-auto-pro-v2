# F-XXX 功能名称

- 状态：preparing
- 产品依据：`docs/PRODUCT.md` 的具体章节
- 架构依据：`docs/ARCHITECTURE.md` 的具体章节
- 计划确认时间：待确认

## 目标

描述这个切片交付给用户的单一结果。

## 范围

- 包含：
- 不包含：

## 完成标准

- [ ] 行为已实现并实际运行。
- [ ] 当前范围测试、类型检查或构建已通过。
- [ ] 已检查同一范围内的相似问题。
- [ ] 文档状态已更新为 `ready_for_manual`。
- [ ] 已列出用户手工核对步骤。

## 状态记录

正常状态按 `preparing -> awaiting_approval -> implementing -> ready_for_manual -> accepted` 推进；`accepted` 必须记录用户明确验收。用户明确反馈人工验收未通过时，允许 `ready_for_manual -> implementing`，必须记录反馈和新的验证结果。

## 人工验收

列出用户可观察、可操作、可判断通过与否的步骤。
