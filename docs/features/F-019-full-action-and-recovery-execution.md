# F-019 全动作与恢复步骤执行

- 状态：ready_for_manual（用户 2026-09-05 裁决：F-019 完成后暂停，先统一 review 再继续）
- 实施基准（2026-09-05）：9 个新增写端点的载荷形状已按参考源码逐一勘定（控制器、DTO 字段、前端实际 JSON、错误文案），记入本切片实施记录；加签的 `auditStatus` 枚举值源码未见明确定义，按 `源码推断、待实测` 处理并如实标注。
- 产品依据：`docs/PRODUCT.md` 第 27 至 29、114 至 118 条（真实动作目录、恢复步骤、导航步骤、辅助流程和动作顺序）；产品原则第 1、5、6 条
- 架构依据：`docs/ARCHITECTURE.md` 的系统边界（目标写请求只能从 `internal/adapter/target` 发出）、`internal/engine` 分包边界和 F-012 场景编译约束
- 纲领依据：`docs/EXECUTION_PROGRAM.md` 第 4.1、4.3、4.4、7.1 至 7.4、8、9、10 节，以及 F-018 对账与安全重试计划
- 前置条件：F-016、F-017、F-018 已完成并经用户明确验收；F-012 的动作配置、人员解析和场景编译已稳定。任何动作仍须通过实时门禁和对账，不能因本切片而绕过前置检查。

## 目标

让 F-012 已编译的 15 类真实用户动作，以及场景编译器生成的恢复、导航步骤，在同一主实例上按目标平台语义执行；需要创建辅助转发流程时，主实例与辅助实例分别记录且可回溯。

## 单一用户结果

用户在一条通过运行前检查的路径上启动运行，执行器根据动作目录逐步完成保存草稿、提交、审批、恢复和导航等步骤。每一步仍走七阶段、一次最多一个写请求；动作不可用时给出目标门禁原因，写结果不确定时进入 F-018 对账。动作执行到哪里、恢复了什么、是否创建辅助流程，都能在运行画布、运行记录和日志中用中文看清。

## 范围

### 包含

- 完整接入动作目录 15 个动作：保存草稿、提交、重新提交、暂存当前表单、加签、移交、同意、不同意、回退上一节点、取回、撤回、催办、转发、关注、取消关注。
- 每个动作的实时门禁、演员策略、参数构造和目标响应映射；共享端点按动作语义区分参数，不用一个通用写请求掩盖差异。
- 场景编译器生成的系统恢复步骤（重提、取回、回退后重走等）和系统导航步骤的真实执行；系统步骤只由编译器产生，用户不能直接配置。
- 转发产生的辅助流程独立登记目标引用、接收人和结果，主实例游标保持不变；主实例与辅助流程的失败分类分开记录。
- 动作执行前重新读取真实节点、待办、演员和表单权限；加签、移交、取回、撤回、回退等动作按当前实例事实而非保存时快照判定。
- 动作级语义证据和 `verifiedRunnableActions` 更新：只有真实跑通并完成日志、目标事实和人工检查的动作才登记为已验证。
- 复用 F-018 的对账、重放、日志和运行画布，不新建动作执行页面。

### 不包含

- 多路径调度、最大并发和定时启动（F-020）。
- 运行分析视图与事件时间线（F-021）。
- 修改目标平台、绕过目标校验、模拟或替换真实处理人。
- 自动跳过门禁、自动重发、批量写入或把 `batchCode` 当幂等键。
- 新增动作目录之外的端点；未能取得语义证据的动作继续阻塞，不以“兼容模式”放行。

## 写端点白名单

本切片允许调用的目标写端点仅为动作目录已声明的 11 个端点：

`/web/flowInstanceApi/submit`、`/web/flowInstanceApi/reSubmit`、`/web/flowInstanceApi/storageFormData`、`/web/flowInstanceApi/approverAppend`、`/flowInstanceApi/audit`、`/web/flowInstanceApi/rollBackThePreviousLevel`、`/web/flowInstanceApi/retrieveProcess`、`/web/flowInstanceApi/revocation`、`/web/urgeHandleRecord/sendUrgeMessage`、`/web/flowInstanceApi/transpond`、`/web/flowInstanceApi/flowTracking`。

契约只允许实际请求集合成为上述集合的子集；读请求仍可调用 F-016 已允许的只读端点。每次尝试最多一次写请求，任何第二次写都必须是 F-018 允许的全新重放尝试。

## 设计要点

- **动作与端点一一受目录约束。** `TargetOperation`、参数白名单、预期效果和重读要求来自 `actioncatalog`；适配层不接受浏览器直接传入的目标 ID、人员 ID 或任意 URL。
- **同一端点不同动作不混淆。** `audit` 根据 `pass`/`no_pass` 处理同意和不同意；`approverAppend` 根据移交或加签的目标协议区分；`flowTracking` 根据关注布尔值区分关注和取消关注。
- **恢复步骤是事实驱动。** 回退、驳回、撤回、保存草稿后的重提，以及同意后同节点继续动作的取回，必须使用编译器输出的恢复步骤，不能靠 Worker 临时插入未记录步骤。
- **辅助流程单独记录。** 转发只记录辅助实例和接收人，不把辅助实例伪装为主实例的下一节点；辅助流程失败不改变主实例游标，但会在路径结果中单独展示。
- **人员连续性不可猜。** 运行时解析不到唯一人员、人员权限变化或演员不再拥有待办时停止并归类为目标拒绝或工具缺陷，不能切换到当前登录账号。
- **每一步都需重读。** 动作成功声明不替代事实重读；状态、节点、待办、演员、已办和实际路径按 F-014/F-018 组合判定。
- **动作登记有证据门槛。** `verifiedRunnableActions` 不是静态全量开关；动作只有在指定测试账号与流程上真实执行成功、日志可回放、目标事实可核对后才写入登记。

## 口径对齐（2026-09-05 核查，多线程实施前必读）

本节由核查线程统一写入 F-017 至 F-023 七个功能文档，内容完全一致，避免各线程口径不一。
与本节冲突的本文件其余表述一律以本节为准。

1. **界面与交互按纲领第 12 节执行。** 该节是跨切片基线（含六条硬性禁止：裸 input、操作堆砌、整页遮罩、
   空白区域、界面暴露内部标识、陈旧文案）。功能文档只写本切片新增的交互，不重复也不弱化基线。
2. **写载荷口径已被 F-024 改写。** 任何携带表单数据的写请求，正文必须是
   「目标实例当前完整表单数据 + 本节点声明可编辑且已配置的字段」，构造入口是
   `internal/engine/step.BuildNodeFormData`；发起没有实例时基线是发起态完整表单模型，
   只去掉「只有后续节点才能编辑」的字段。禁止直接透传 `runCtx.EffectiveFormData`
   （目标保存表单数据是整份覆盖，见语义清单第 16 条）。
3. **节点标识口径。** 编译场景与配置快照里的 `nodeKey` 是哈希派生的不透明键，不能发给目标。
   凡是与目标对照（待办读取、按节点写参数、事实重读、对账）都必须用 `NodeInfo.TargetNodeID` /
   `StepPreview.TargetNodeID`；任务级动作拿不到真实标识时必须阻塞，不得用空值比对。
4. **迁移编号。** 现有最大编号为 `028`（F-018）。新增迁移从 `029` 起，编号一律不复用。
5. **语义清单现状。** `docs/TARGET_SEMANTICS.md` 已有 17 条（新增第 15 条手动分支提交传参、
   第 16 条表单数据整份覆盖、第 17 条条件求值数据来源），证据块共 30 个，
   漂移检测脚本 `test/contracts/f014/semantics_evidence_drift.sh` 已在用：新增语义一律扩展它，不另造。
6. **动作与写端点的准确数字。** `internal/model` 有 16 个 `ActionKey`；写端点共 11 个
   （`write.go` 2 个 + `write_actions.go` 9 个）。任何计划里的动作数、端点数以此为准。
7. **控制端点当前按运行 ID 寻址。** `RunOrchestrationService.resolvePathRunID` 经
   `GetPathRunByRun`（SQL 为 `ORDER BY id LIMIT 1`）解析，全部 8 个控制端点共用。
   这是一次运行只跑一条路径的前提；F-020 引入多路径时必须先改成按路径运行寻址。
8. **既有实现必须先读后写。** 实施前先用 `git log --oneline` 与目录清单确认本切片是否已有落库实现，
   已有实现按「补齐与返工」推进，不重建第二套；重复实现属于交付缺陷。

### 本切片现状（核查线程如实记录）

后端已落库（提交 `14cf365`）：`internal/adapter/target/write_actions.go` 提供 9 个写端点
（与 `write.go` 的发起、审批合计 11 个），`test/run-f019.sh` 与 `test/contracts/f019/action_whitelist.sh` 已存在。
因此本切片不是从零开始，按「补齐与返工」推进。

必须补齐的具体缺口（核查已确认）：

- **不同意（`reject`）没有对应载荷分支。** `internal/engine/step/gate.go` 把它路由进
  `target.ActionWriteRequest`，而 `target.BuildActionBody` 的 switch 里没有 `reject` 分支，
  会落到 `default` 返回 `UNSUPPORTED_ACTION`。当前是失败安全（不会误写），但动作全集并未真正齐备。
  不同意与同意共用 `/flowInstanceApi/audit`，差别只在 `auditRecord.auditStatus=no_pass`。
- 全部带表单数据的动作（发起、同意、不同意、重新提交、暂存、转发）都必须走口径对齐第 2 条的载荷规则，
  不得再直接透传历史快照；`storage_form_data`、`transfer`、`add_sign` 的节点级参数必须用真实节点标识。
- 加签的 `auditStatus` 取值仍是源码推断，需在真实写覆盖到时补进语义清单并改写证据强度。

## 详细执行任务

每个任务完成后独立验证并使用中文提交说明；实施前必须确认 F-018 已验收。

## 实施记录（2026-09-05，停在 ready_for_manual）

- T01 完成：`internal/adapter/target/write_actions.go` 统一动作写出口 `ExecuteActionWrite` 与载荷构造器 `BuildActionBody`，
  覆盖重新提交/暂存/加签与移交（approverAppend，auditStatus 区分）/回退/取回/撤回/催办（不走 data 容器的独立协议）/转发/关注与取消关注（顶层 tracking 布尔）九类，
  端点常量共 11 个，载荷形状与参考源码及前端实际发送逐字同源。
- T02 部分完成：演员-待办新鲜复验覆盖审批/回退/取回（待办任务 ID 发送前现场读取）；已办记录与动作痕迹的只读读取尚未接入
  （目标 flowJobTaskLink 接口需要待办人会话视角，见 F-018 收集器的如实标注），因此「取回」类恢复步骤的真实命中暂不可构造。
- T03 部分完成：导航步骤（system_navigation）已按只读校验语义接入七阶段（不发写请求、事实可读即通过、独立 step/attempt/日志）；
  恢复步骤按其动作字段分派（审批类恢复已可执行；取回/重提恢复步骤受上述读取接入限制）。
- T04/T05 完成：表单数据按原始字节透传；转发辅助流程的载荷与业务关联已实现，独立事实行登记待真实转发运行验证。
- T06 如实记录：`verifiedRunnableActions` 仍只登记「提交」（F-016 实测）；其余 14 个动作的执行代码已就绪但**未逐动作真实跑通**，
  按本文件「动作登记有证据门槛」的设计不登记；真实逐动作验证需要可污染流程与人工配合，留待统一 review 后按批次进行。
- 已知边界：未接入已办读取前，取回/回退后重走等恢复步骤在真实环境会以中文原因停止（「已无本演员在本节点的活动待办」或门禁原因），不会盲目重发。

### T01：动作执行契约与端点适配

为 15 个动作建立不可变的目标操作契约、参数白名单、响应解析和重读要求。补齐适配层不重放写入口，保留传输阶段事实，并用编译时和运行时校验拒绝未登记动作、目标临时标识和 `batchCode`。

### T02：演员、会话与权限复验

把 F-012 人员策略解析接入每个动作的 `prepare` 阶段；覆盖固定人员、角色、组织范围、运行时关系和移交后的新演员。会话失效只能在写请求发出前按只读规则重登，写请求发出后不自动重发；所有失败写入同名分类。

### T03：恢复步骤真实执行

把 `scenario` 生成的重提、取回、回退后重走、同节点取回和导航步骤接入七阶段执行器。每个系统步骤有独立 `step_id`、`attempt`、日志和事件，执行顺序与用户动作顺序可回放，恢复失败不得静默删除或替换。

### T04：动作参数与表单数据桥接

从历史数据工作区取得原始表单数据和当前路径补丁，按动作权限决定可编辑字段；区分发起保存草稿、审批暂存和审批提交。附件、富文本、选项字段和审批意见按目标协议原样传递，禁止重新序列化导致数字或嵌套结构变化。

### T05：转发辅助流程与结果落账

实现转发的受限接收人选择、辅助实例创建和只读重读；主实例、辅助实例和接收人使用独立事实行及事件关联。明确辅助流程失败、主流程继续和主流程写结果不确定三种结果，不把辅助实例写入主实例当前节点。

### T06：动作验证登记与运行准备解锁

为每个实际跑通动作建立语义清单条目映射、目标部署版本、参考仓库 HEAD、运行号和日志引用；运行前检查只解锁已有证据的动作。未验证动作继续以提醒或阻塞方式呈现，不能因“端点相同”自动解锁同端点的其他动作。

### T07：前端动作与恢复状态展示

在现有运行画布侧栏显示动作中文名、演员、参数摘要、七阶段结果、恢复来源、辅助流程和对账入口；系统步骤明确标注“系统恢复”或“系统导航”，不能伪装成用户配置动作。沿用唯一动作约束，不增加跳过或任意节点操作。

### T08：测试、真实验证与文档交接

新增 `test/unit/backend/action_execution/`、`test/integration/f019_*`、`test/contracts/f019/` 和 `test/run-f019.sh`。纯逻辑覆盖 15 动作门禁、参数、恢复顺序和失败分类；真实写按动作逐个使用用户指定账号和可污染流程执行；白名单脚本、日志双向引用、单次写请求和动作登记证据全部检查。同步 `docs/TARGET_SEMANTICS.md`、本文件、路线图和进度。

## 完成标准

- [ ] 界面与交互逐条对齐 `docs/EXECUTION_PROGRAM.md` 第 12 节基线：无裸 input／无操作堆砌／无整页遮罩／
      无空白态／界面不暴露内部标识／无陈旧文案；等待过程显示阶段与重试、读数同源且单调不减、
      不可逆操作有明确前置说明、自动跟随可接管、可键盘聚焦、状态不只靠颜色。
      定向验证入口含界面结构契约脚本反向断言，人工验收逐条给出可判断的交互步骤。
- [ ] 15 个动作均有目标端点、参数、门禁、预期效果和重读要求，目录与执行器没有第二套动作定义。
- [ ] 每个动作可在目标允许的真实上下文执行；不满足门禁时明确阻塞，不能跳过或换演员。
- [ ] 恢复步骤和导航步骤按编译场景真实执行，每一步都有独立事实、日志和事件；顺序错误在执行前阻止。
- [ ] 转发创建的辅助流程与主实例分开记录，辅助失败不伪装成主实例状态变化。
- [ ] 每次尝试最多一次写请求，写结果不确定统一进入 F-018 对账；无自动重发和静默重试。
- [ ] `verifiedRunnableActions` 只包含已有真实证据的动作，同端点不同动作不会被连带解锁。
- [ ] 表单、附件、富文本和数字数据保持目标原始结构；写请求不带 `batchCode` 和未授权临时标识。
- [ ] 运行画布能显示用户动作、恢复、导航、演员、门禁、结果和辅助流程，不新建运行详情页。
- [ ] `test/run-f019.sh` 实际通过，单测、集成、契约、前端构建无跳过；真实写证据逐动作登记。
- [ ] 实际写请求集合是 11 个白名单端点的子集；目标语义条目、参考 HEAD 和部署版本已同步。
- [ ] 已检查同范围相似问题：重复端点参数、旧动作别名、系统步骤重复插入、转发混入主实例和写路径会话重放。
- [ ] 文档状态更新为 `ready_for_manual`，并列出用户手工验收步骤。

## 未决与需要用户裁决的事项

1. 转发辅助流程失败时默认不阻塞主实例后续步骤，但在路径结果中保留独立失败项；若用户要求主流程随辅助流程失败一起停止，实施前需调整状态聚合规则。
2. 15 个动作的真实验证可以分批完成，但每个动作都必须有独立证据；未完成动作不因同端点已有证据而解锁。

## 状态记录

正常状态按 `preparing -> awaiting_approval -> implementing -> ready_for_manual -> accepted` 推进。

- 2026-09-05 `preparing` -> `awaiting_approval`：依据 F-012 动作目录、场景编译实现、F-016 七阶段、F-017 控制、F-018 对账计划和纲领第 9 节补齐本文件；本次只写计划，不实现代码。
- 门禁：必须在 F-018 完成并经用户验收后才能实施；真实写仍受用户指定账号、动作级证据和端点白名单约束，完成后停在 `ready_for_manual`。

## 人工验收

1. 逐项打开 15 个动作的运行记录，确认中文动作、演员、目标效果和重读依据与实际操作一致。
2. 在一个需要重提或取回的场景中，确认系统恢复步骤被单独显示和记录，用户动作顺序没有被隐藏步骤改写。
3. 执行加签、移交和转发场景，确认候选受实时门禁限制；转发产生独立辅助流程，主实例游标不被伪造推进。
4. 检查审批暂存与发起保存草稿的请求和结果不同，暂存不推进任务，草稿后续重提使用同一主实例。
5. 检查一个未登记动作仍被运行前检查阻塞；确认同一端点的已登记动作不会解锁它。
6. 打开 `step.log`、`network.log`、`curl.log` 和运行详情，确认每一步只有一次写请求且可回放。
7. 在项目根执行 `./test/run-f019.sh`，确认无跳过并输出动作级白名单与真实验证结果。
