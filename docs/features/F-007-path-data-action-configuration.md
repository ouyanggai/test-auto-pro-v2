# F-007 已保存路径的节点可视化配置

- 状态：implementing
- 产品依据：`docs/PRODUCT.md` 的“路径生成与节点配置边界”“路径节点可视化配置行为（F-007）”
- 架构依据：`docs/ARCHITECTURE.md` 的“F-005 执行路径选择与持久化”“F-006 路径要求分析”“F-007 已保存路径的节点可视化配置”
- 重新规划时间：2026-08-10

## 方向纠正

现有 F-005 路径生成、分支选择、路径命名、保存、编辑、复制和删除交互已经符合需求，本轮不得返工或替换。

此前 F-007 把字段和动作放进独立表单式配置页面，导致用户离开流程结构后无法判断正在配置哪个节点，也无法自然承载人员、断点和运行状态。之后虽然改为节点画布，但仍把 FormMaking 模板拆成 Naive UI 基础控件放在节点侧栏，并以一条路径配置记录代表整条路径完成。用户在第三次人工验收中明确拒绝该行为。

## 第三次人工验收退回诊断

- 当前 V2 没有接入 `rsh-flow-components` 使用的 FormMaking 运行时。后端只抽取字段元数据，前端 `NodeConfigurationPanel` 再自行渲染 `NInput`、`NSelect` 等基础控件，因此丢失真实表单布局、自定义组件和运行逻辑。
- 保存请求已经成功写入 `test_execution_path_configs` 后，前端 `applyPathConfigDraft` 对 Vue 响应式代理执行 `structuredClone`，浏览器会抛出 `DataCloneError`。异常被统一包装成“保存失败”，刷新后又因为数据库记录真实存在而显示 configured。
- `FlowGraphCanvas` 同时设置 `elements-selectable=false`、`nodes-draggable=false` 且没有注册 Vue Flow 节点事件。Vue Flow 因此给节点包装层计算出 `pointer-events:none`，内部按钮无法接收 hover 或 click，节点详情不能切换。
- 当前保存接口整份替换路径配置并固定写入 `config_status='configured'`；路径列表只判断配置行是否存在。一次点击“保存节点配置”会让整条路径和所有默认动作节点被视为已完成，无法表达逐节点进度。
- 节点固定高度内同时排版类型、名称和状态标签，状态文字与名称争抢空间；表单字段又占满侧栏，使人员、动作和节点规则失去层级。

本轮必须同时纠正表单运行时边界、节点点击与状态呈现、逐节点保存和服务端对账语义；只修 CSS 或错误文案不能通过验收。

## 单一用户结果

用户先生成并保存执行路径，再进入该路径的节点画布：点击节点只配置人员、动作和节点规则；通过独立“配置表单数据”入口打开真实 FormMaking 表单工作区。每次保存只更新当前节点或表单区域，界面始终准确显示未配置、部分完成、已配置或配置失效，不再出现前端报失败而刷新后悄悄完成。

## 用户流程

1. 计划没有已保存路径时，路径准备区域只说明“请先配置并保存执行路径”，主操作为“配置路径”，进入现有 F-005 路径生成交互；不显示无效的节点配置入口。
2. 计划存在已保存路径时，列表继续显示稳定序号和名称，每条路径增加明确的“配置节点”入口及节点配置状态；不把“编辑路径”和“配置节点”混为同一操作。
3. “编辑路径”继续进入现有 F-005 线路选择，用于改变分支和路径名称；“配置节点”进入 `/plans/:planId/paths/:pathId/configure`，用于配置这条已保存路径上的节点信息。
4. 节点配置页面以独立画布为主体。顶部提供返回计划详情、当前路径、逐节点进度和单独的“配置表单数据”；中间显示真实流程结构和当前路径；固定右侧面板只显示被选节点的人员、动作与规则。
5. 返回计划详情后保留原路径列表和流程位置；切换另一条路径时重新加载该路径自己的拓扑投影与节点配置，不能串数据。
6. “配置表单数据”打开隔离的真实 FormMaking 工作区；关闭后回到原画布位置，不把表单字段复制进节点侧栏。
7. 新发起表单可基于模板、近期样本、发起人和当前路径条件执行“智能生成”或“换一组”，用户在真实表单中修改后独立校验保存；已发、待发继续只读显示实例当前值。

## 节点配置画布

- 使用 F-004 的同一棵真实流程树和 F-005 的同一路径选择结果，不建立第二套拓扑模型。当前路径高亮，非当前路径节点作为流程上下文弱化并保持只读。
- 只有当前路径上、且目标模板证明可配置的业务节点可以打开配置；并行、汇聚、空节点和结束节点没有业务配置时只展示说明，不生成伪配置项。
- 节点状态通过名称区域之外的状态点、角标或边框表达，不在固定高度内增加一整行标签。页面顶部汇总节点总数、已完成数和待处理数，并能定位下一待配置节点。
- 点击节点后在画布内部固定右侧面板展示配置；当前路径可配置节点必须真实接收 pointer、hover、focus、click 和键盘操作，选择节点不得导致画布尺寸反复跳动。
- 右侧面板只显示当前节点实际需要的区域，内部独立滚动；没有选中节点时给出明确引导并自动将下一待配置节点定位到方便操作的位置。
- 节点配置成功后刷新节点状态，并自然引导到下一待配置节点；失败时保留当前输入和选中节点，把错误定位到具体配置项。

## 模板约束下的节点内容

- 发起节点：显示发起人规则、独立表单数据入口、提交动作，以及模板允许在发起阶段确定的下一步人员或分支信息；表单字段本身不在节点侧栏展开。
- 审批与协同节点：显示真实审批方式、会签/竞签人数、跳过规则、允许的处理结果和处理人来源。固定人员、角色或组织规则只读展示；只有模板允许“发起时指定”或“运行节点选择”时才显示受约束的人员选择器。
- 条件分支：展示已保存路径选择的真实条件和驱动字段。需要修改线路时返回“编辑路径”；需要影响条件时打开独立表单数据工作区，不能在节点侧栏直接编辑字段编码或内部判断表达式。
- 手动分支：展示当前已保存分支；只有目标模板和当前阶段允许重新选择时才给出合法候选，否则引导返回“编辑路径”。
- 目标平台自动确定、依赖组织关系或只能在实例运行时计算的人员，明确标记为只读或“运行时确定”，不伪造全员候选列表。
- 表单、人员、动作和分支都由后端读取当前目标模板后投影。未知控件、权限不明或目标结构变化时阻止该项完成，不按名称猜测，不用普通输入框冒充真实业务组件。

## 断点、暂停与单步边界

- 节点配置画布是后续运行控制的共同视觉载体，组件必须预留“配置态”和“运行态”，避免以后再做一套与流程脱离的操作页面。
- F-007 只完成运行前的节点配置，不显示无法真实生效的暂停、继续或单步按钮，也不创建虚假的运行状态。
- 后续真实运行时在同一流程画布显示当前、成功、失败、等待状态，并提供暂停、继续、执行下一步和本次运行临时断点；单步一次最多执行一个原子动作后重新暂停。
- 本次运行临时断点属于运行记录，不回写已保存路径。若以后需要“每次运行都在此暂停”，必须作为独立的固定暂停配置命名，不能与临时断点共用语义。

## 数据与组件边界

- `PlanPathsView` 继续负责计划详情、路径列表和现有 F-005 路径编辑入口；无路径时引导配置路径，有路径时提供逐条“配置节点”。
- `PlanPathConfigurationView` 负责节点画布和独立表单工作区的协调；`NodeConfigurationPanel` 只消费人员、动作和规则，不再渲染表单字段。
- `form-runtime/` 的实际 dev/build 源码位于 `runtime-source/`，由 `参考代码/rsh-flow-components` 当前已核实的完整 tracked 源码、原生同步资产与真实 FormMaking 组成；当前落库基线为 `bff4ef8b938db5578c3f7eab1f482a4e9388917c`。主 Vue 3 只通过 iframe 和版本化 `postMessage` 契约通信。
- 真实入口沿用 `rsh-flow-components` 的 FormMaking、自定义组件注册、Vuex、router、axios 和表单样式；`form-runtime/src/`、根 `scripts/`、构建配置、iframe/SID/写阻断适配属于本项目保护区。配置阶段禁用原工作台登录与不安全消息入口并阻断流程/业务写操作；确实依赖外部宿主且未完成独立适配的组件明确 `unsupported`，不能降级成普通控件。
- 主应用把当前已验证账号缓存的 SID 和核实目标网关放入单次 iframe 会话，使目标表单组件可执行必要只读请求；切换计划、路径、账号或关闭工作区时销毁会话并清除 SID/上下文，消息必须核对 origin、source、session 和协议版本。SID 不写入 Git、路径配置、维护任务或浏览器长期存储；配置阶段阻断目标流程提交、草稿和业务写端点。
- 复用现有路径配置表、修订号和幂等能力，但增加配置模式版本、逐节点确认集合、表单数据与表单状态。旧版配置记录保留数据并标记为 `affected`，不得继续按“存在一行即已完成”投影。
- 节点与表单分别保存：节点请求只合并当前节点的人员和动作，表单请求保存 FormMaking 校验后的路径级模型。前端只提交不透明键；服务端返回并重建权威状态，不能在浏览器本地把所有节点批量改成已完成。
- 保存请求必须传入取消信号；响应丢失或客户端后处理异常时用原幂等键重试或 GET 对账。只有服务端确认未保存时才展示失败，成功但响应不确定时明确提示已核对的真实结果。
- 基础智能生成复用 V1 已验证的递归模板解析、样本类型画像、稳定 seed、路径条件约束和人工覆盖合并思想，但按 V2 数据模型重写。样本只经目标适配层限量读取近期可见已发实例，禁止直连 Mongo；缺少样本时明确降级到模板约束与确定性兜底。
- 生成结果必须经真实运行时 `setData`、`refresh` 后由 `getValues()` 捕获完整数据；“换一组”只改生成器拥有字段，人工修改字段不覆盖。服务端保存 seed、`generatedFieldPaths`、`manualOverridePaths`、样本来源摘要、校验结果和表单状态。

## 不包含

- 不修改 F-005 的路径生成、分支选择、路径命名、保存、编辑、复制、删除和全屏交互。
- 不把“配置节点”塞进 F-005 的路径生成面板，也不要求用户在生成路径时顺便配置节点。
- 不调用目标平台提交、审批、人员写入或业务保存接口，不创建运行记录。
- 不在本轮实现真实暂停、继续、单步、临时断点或运行调度；只保证节点画布可以在后续复用于运行态。
- 不复制 V1 的动作程序树、动作库拖拽、全局人员池或复杂工作室布局。
- 不把旧 `rsh-flow-components` 工作台页面作为本产品业务入口；运行时只复用目标组件必需的 Vuex/router/axios 内部链路并进入独立配置路由，不启用旧登录流程、目标流程提交或业务写操作。
- 不在本轮实现高级 AI 语义生成、跨模板学习或未知复杂业务组件的自动适配；基础智能生成只覆盖模板和真实样本能够安全证明的字段与路径条件。

## 完成标准

### 产品行为

- [x] F-005 路径生成与线路编辑零回退；无路径仍先配置路径，有路径仍逐条进入节点配置画布。
- [x] 节点侧栏彻底移除表单字段控件，只保留当前节点人员、动作、模板规则和当前节点保存。
- [x] 当前路径可配置节点具有清晰 hover、focus、click、选中反馈；点击后侧栏立即切换。状态角标不与节点名称重叠。
- [x] 独立“配置表单数据”使用真实 FormMaking 引擎还原当前样本的布局、控件、默认值与校验；主 Vue 3 页面不自行模拟表单组件。
- [x] 新发起可用模板、近期样本、发起人和当前路径条件执行可复现的智能生成；用户可修改，“换一组”保留人工覆盖，并显示自动填充与仍需手工数量。
- [x] 新发起显示模板默认表单并保存路径级测试数据；已发、待发显示实例当前值且本轮只读。自定义组件缺少适配时原位说明，不伪造控件。
- [x] 保存当前节点只完成当前节点；首次保存后路径显示“部分完成”，只有表单和所有必需节点完成后才显示“已配置”。旧错误记录显示“配置失效”。
- [x] 保存成功、响应丢失重试和刷新后的状态一致；不能出现页面报失败但服务器已经保存，也不能一次保存把全部节点改成已完成。
- [x] 深浅主题、表单工作区关闭返回、较大流程、路径隔离和运行假按钮边界符合现有约束。

### 自动验证

- [x] Form runtime 单测覆盖模板默认值、数据回填、校验、只读模式、unsupported 自定义组件和 postMessage 会话隔离。
- [x] 生成器单测覆盖递归模板/样本画像、稳定 seed、选项/日期/人员、路径条件与默认分支、人工覆盖合并、换一组和样本不足。
- [x] 前端回归覆盖 Vue 响应式对象保存、服务端对账、节点真实点击事件、单节点状态更新、表单工作区和路径间隔离。
- [x] 后端单元与契约测试覆盖节点合并保存、表单独立保存、路径四态与表单五态、幂等对账、旧配置 affected 和目标结构变化。
- [x] 使用隔离临时数据库实际运行迁移与存储集成，不再因未导出 `PLAN_DB_*` 跳过本轮核心持久化验证。
- [x] `pnpm dev:f` 的命令结构同时持有主前端与 19001 独立表单运行时；主前端、表单运行时类型/完整生产构建、Go build、必要 F-004/F-005/F-006 回归和 `git diff --check` 全部通过。
- [x] 运行时维护固定仓库/远端/分支、任务创建时 HEAD、脏源拒绝、完整映射/摘要校验和本地适配保护均有根目录测试；不能把一次性复制脚本作为同步正确性来源。
- [x] 维护任务完整执行 `INSPECT → SYNC → SYNC_CHECK → BUILD → RESTART → VERIFY → COMPLETED`；单活动任务、租约/fencing、Worker 恢复、有界日志、候选构建失败不影响 current、切换/健康失败恢复 previous 和 pnpm 健康检查均有自动证据。
- [x] 自动验证完成后停在 `ready_for_manual`，不自动启动浏览器，不开始后续运行功能。

## form-runtime 来源与维护决定

- `form-runtime/sync-manifest.json` 固定来源仓库、远端、`master` 与允许映射，不永久写死历史 HEAD；`make refs-sync`/`make refs-status` 负责参考仓库的分支与快进安全，创建任务时持久化当刻干净 HEAD，Worker 执行前复核同一快照。API 不允许请求提交路径、分支、HEAD 或命令。
- 同步完整 tracked `src/`、`public/`、原生 `scripts/sync*.js`、上游清单和构建资产，真实构建直接消费 `runtime-source/`。上游原生 35 项映射资产仍保留供来源追踪；当前项目清单负责从已核实的组件仓库形成候选。`.npmrc`、Git 元数据、依赖、构建产物和凭证不复制，本地适配不在同步覆盖区。
- 维护状态机复用旧 V2 ADR-0016 的来源快照复核、单活动任务、租约/fencing、在线日志、候选构建、失败回退和崩溃恢复语义。当前项目用 pnpm 版本目录替代 Docker：候选在 `.runtime` 隔离同步/构建，成功后才同时切换实际 dev 输入与生产 `web/dist/form-runtime`；真实 HTTP 必须报告同一 HEAD/摘要才可完成，否则恢复并复核 previous。
- 系统设置提供唯一维护入口，展示固定来源、分支、任务 HEAD、dirty、检查时间、阶段、结果、恢复结果与有界日志；只有“一键同步并更新运行时”，不提供任意命令输入。SID/表单会话和同步维护完全分离。

## 第四次人工复核退回完成标准

- [x] `form-runtime/` 的真实 dev/build 入口直接消费完整 `rsh-flow-components` 可运行源码、原生自定义组件注册与真实 FormMaking 链路；删除闲置 upstream、独立 vendor 和空组件注册组成的平行运行时。
- [x] 保留并适配上游 `scripts/sync*.js`、`sync-manifest.json` 及构建资产；同步后的代表性源码变化进入实际候选构建输入和产物。
- [x] 来源固定为规范仓库、远端与 `master` 分支；创建维护任务时记录当次干净 HEAD，Worker 执行前复核同一快照，后续经 `make refs-sync` 安全快进的新 HEAD 可创建新任务。
- [x] 开发态同步替换 19001 Vue CLI 服务的实际监听源码并等待其 HTTP 快照；生产候选切换作用于主站 `/form-runtime/`，只有 HTTP HEAD/摘要匹配才能完成，失败恢复 previous 并再次核验。
- [x] SID 会话传入、销毁、消息隔离、目标业务写阻断与完整 `getValues()` 保存保持有效；目标自定义组件不再被统一清空或降级成普通控件。
- [x] 根目录测试覆盖完整同步资产、真实入口、原生组件注册、创建时 HEAD、执行时来源变化拒绝、候选产物变化、开发服务更新、生产 HTTP 健康、current 保护、previous 回退与维护事务状态机。
- [x] 隔离临时 MySQL、`./test/run-f007.sh`、Go build、主前端与表单服务构建、必要 F-004/F-005/F-006 回归和 `git diff --check` 全部通过，状态恢复 `ready_for_manual`。

## 本轮独立复核修复标准

- [ ] 运行时请求策略按目标 URL、HTTP 方法和已证明语义分类；不以“POST 即写入”误伤合法只读查询。
- [ ] 真实组件已发现的流程提交、草稿、业务保存/更新/删除、上传和文件关联接口均由 XHR 与 fetch 统一阻断；无法证明为只读的目标请求不得静默放行，并返回稳定 unsupported/拒绝错误。
- [ ] 已证明的目标只读 POST 查询仍能附加当前会话 SID；销毁策略后 XHR、fetch 和原请求头行为恢复原状。
- [ ] 根目录定向测试覆盖已知写路径、代表性自定义组件写路径、只读 POST、策略销毁和 F-007 全量门禁；验证后状态回到 `ready_for_manual`。

## 已被本轮人工验收否决的旧实现

- 后端继续复用现有路径配置表、修订号、幂等保存和三类来源真实目标读取；配置投影新增稳定不透明节点键、节点进度与状态，并复用 F-006 条件、人员和动作要求。模板固定人员、角色和组织范围保持只读；只有目标详情实际返回合法候选时才允许人员选择，未知或运行时规则不伪造候选。
- 前端 `/plans/:planId/paths/:pathId/configure` 已改为复用 F-004 结构化树布局和 F-005 已保存路径的 Vue Flow 画布。当前路径高亮、路径外上下文弱化，业务节点与路由节点显示配置状态；选择节点只替换画布内固定右侧面板，不重置缩放或视口。
- 右侧面板按当前节点展示后端投影的模板要求、基础字段、人员规则、标准动作和不可编辑缺口。日期与日期时间继续使用严格 Naive UI 控件；保存成功重建节点状态索引，保存失败或目标结构变化保留仍可安全对应的草稿与选中节点。
- 可跳过的会签人员选择现在明确区分“零人跳过”和“主动选择”：零人仍合法，一旦选择则前后端都要求一次达到模板最低人数；未满足时以人员标题提示并拒绝保存。
- 刷新存量配置时复用保存端的同一人数规则：误存的不足人数、模板最低人数提高或超过当前上限都会标记人员与节点“配置失效”，候选失效处理保持不变；节点状态不再另行推导一套人数规则。
- 计划详情无路径时只显示“配置路径”并进入既有 F-005 线路交互；有路径时逐条显示“配置节点”和本地配置状态。F-005 的新增、命名、分支选择、保存、编辑、复制、删除和页面全屏状态机未改写。

## 第四次实施结果

- 后端新增表单生成/保存分域和逐节点合并保存，完整 FormMaking values、seed、生成/人工路径、样本摘要、五态表单与路径状态均由服务端持久化并复验；同一幂等键并发首次保存通过 no-op upsert 收敛为同一修订，不同键仍保持修订冲突。
- 主前端配置页复用当前路径流程树，节点可真实 hover/focus/click 并切换固定面板；节点侧栏只显示人员、动作和规则。表单数据切换到独立 iframe 工作区，提供智能生成、换一组、恢复已保存和独立保存，网络结果不确定时使用原键重试/GET 对账。
- `form-runtime/` 已由完整 `runtime-source/` 驱动真实 Vue CLI dev/build；实际入口加载原生 FormMaking、20 个目标自定义组件、Vuex/router/axios 与样式，本地只追加 iframe 会话、SID 内存认证、写阻断和配置路由。闲置 upstream、独立 vendor 与空组件表已删除。
- 表单运行时维护采用持久化七阶段状态机、动态任务 HEAD、完整目标摘要、隔离源码/产物候选、真实 HTTP 快照、previous 回退、租约/fencing、Worker 恢复和有界日志；代表性源码变化已证明进入候选产物，系统设置只有固定“一键同步并更新运行时”。

## 第四次自动验证结果

- `./test/run-f007.sh`：全部通过，包含 F-007 后端单元/API/目标适配/前端/运行时/维护流水线、F-004/F-005/F-006 必要回归、Go build、双前端类型检查与生产构建、真实来源 `SYNC_CHECK` 和 `git diff --check`。
- 本次从本机忽略文件加载 `PLAN_DB_*`，在隔离临时 MySQL 实际完成配置表与维护任务表迁移、逐节点/表单持久化、幂等并发、级联、单活动任务、租约和 fencing 验证；临时库按测试清理。
- 未启动浏览器；真实目标模板、自定义组件、视觉、iframe 高度及现场维护回退由 `test/manual/F-007.md` 人工核对。
- 真实 `pnpm --dir form-runtime sync`/`sync:check`、完整 Vue CLI 表单运行时生产构建与构建包关键入口检查通过；构建仅保留目标源码本身，生成的 `web/dist`、`.runtime/server` 已清理。

## 旧实现的自动验证结果（不代表人工通过）

- `./test/run-f007.sh`：通过，覆盖 F-007 后端单元、API 契约、目标适配器集成、前端纯逻辑与结构检查，以及必要 F-004/F-005/F-006 回归。
- `go build -o .runtime/server ./cmd/server`、`pnpm --filter test-auto-pro-v2-web typecheck`、Vite production build、`git diff --check`：均通过。
- 本机未配置 `PLAN_DB_HOST`、`PLAN_DB_USER`、`PLAN_DB_PASSWORD`，MySQL 集成按脚本门禁明确跳过；未启动浏览器，真实视觉与目标数据由人工清单复验。

## 本轮审查修复标准

- [x] 可跳过的人员选择允许保持零人；一旦选择人员，无论是否必选都必须达到模板 `minCount`。
- [x] 后端保存校验与前端即时完整性判断使用同一规则，并保留候选范围、去重和最大人数边界。
- [x] 根目录测试覆盖 optional 人员选择 0/1/2 人及 required 原有行为。
- [x] 完整 `./test/run-f007.sh` 通过后重新停在 `ready_for_manual`。

## 第二轮审查修复标准

- [x] 存量人员配置统一使用同一人数规则：optional 零人合法，非零低于 `minCount`、超过 `maxCount` 或 required 零人均标记受影响；候选失效边界保持不变。
- [x] 节点状态复用同一纯规则，存量无效人员配置不得继续显示“已完成”。
- [x] 根目录 analyzer 测试覆盖 optional 0/1/2、required 0 和超过上限。
- [x] 完整 `./test/run-f007.sh` 通过、构建产物清理后重新停在 `ready_for_manual`。

## 人工验收

1. 新计划没有路径时，只能先点击“配置路径”并使用现有路径生成交互；保存路径后列表出现“配置节点”。
2. 打开新发起路径的“配置表单数据”，确认看到目标真实 FormMaking 布局、三个当前样本字段和模板默认值，而不是侧栏中的三个 Naive UI 输入框；保存后关闭并重新打开，数据按当前路径恢复。
3. 打开已发或待发路径的表单数据，确认使用实例当前值且本轮只读；自定义组件没有适配时在原位置明确提示，不出现伪造输入框。
4. 依次 hover、键盘聚焦并点击发起、审批、条件、手动分支、并行/结束节点；可配置节点反馈明显，右侧立即切换，状态角标不遮挡节点名称。
5. 右侧面板只包含当前节点人员、动作和规则。保存一个节点后只有该节点完成，路径显示部分完成；刷新后状态不变，继续完成剩余节点和表单后路径才显示已配置。
6. 模拟保存响应丢失或连续点击，确认界面通过幂等键和 GET 对账得到服务器真实结果，不出现“显示失败但刷新后已配置”。
7. 检查旧版路径配置显示配置失效并要求重新确认；打开两条路径确认表单、人员、动作与状态互不串联。
8. 核对深浅主题、表单工作区关闭返回、画布缩放拖动、长名称和较大流程，并确认 F-005 新增、编辑、复制、删除、线路选择和页面全屏无回退。

## 实施约束

- 本轮涉及独立 Vue 2 FormMaking 运行时、iframe 通信、Vue Flow 交互修复、目标模板适配、存储迁移、逐节点事务合并和旧数据兼容，评估为高复杂度、高不确定性。
- 用户批准后只派发到固定实施任务 `019fa1d9-4b72-7932-9452-b7b66354993e`，指定 `gpt-5.6-sol / xhigh`；不得改用当前主任务、子智能体或其他临时任务编码。
- 实施使用 `.agents/skills/implement-feature-slice`，完成后主任务使用 `.agents/skills/review-feature-slice` 独立复核。
- 使用中文原子提交；不自动启动浏览器，完成后停在 `ready_for_manual`，不得自动开始后续运行功能。

## 状态记录

- 2026-08-10（人工验收退回）：用户确认现有 F-005 路径配置逻辑没有问题；问题仅在已保存路径的节点配置。原独立表单式配置页面无法表达节点上下文、模板约束和未来运行控制，本轮退回重新规划。
- 2026-08-10（重新规划）：明确保留 F-005 全部路径交互；无路径先引导配置路径，有路径从列表逐条进入独立节点配置画布。F-007 进入 `awaiting_approval`，等待用户批准后再实施。
- 2026-08-10（重新实施）：用户明确批准开始实施。固定实施任务按本文完成标准复用 F-004 真实流程树、F-005 已保存路径和既有配置持久化，改造为按真实节点投影与画布内固定面板；保持 F-005 全部线路交互，不增加运行控制或目标写接口。状态进入 `implementing`。
- 2026-08-10（重新实施完成）：按真实节点完成后端状态、要求与受限人员投影，前端配置页替换为同一 Vue Flow 节点画布和固定右侧面板；完整 `./test/run-f007.sh` 通过，MySQL 集成因本机未配置 `PLAN_DB_*` 按约定跳过。状态进入 `ready_for_manual`，等待人工验收，不开始后续运行功能。
- 2026-08-10（独立复核退回）：发现允许零人员跳过的会签节点在用户已选择人员时没有继续执行最低人数约束，“至少 2 人”会错误放行 1 人。F-007 退回 `implementing`，只修正前后端同一人数校验边界并补定向回归。
- 2026-08-10（最低人数修复完成）：后端保存与前端即时完整性均已落实“可跳过时零人合法、非零必须达到最低人数”，optional 0/1/2 人和 required 回归通过；完整 `./test/run-f007.sh` 通过，本机未配置 `PLAN_DB_*`，MySQL 集成按约定跳过。状态重新进入 `ready_for_manual`。
- 2026-08-10（第二轮独立复核退回）：发现修复前误存或目标最低人数变化后的存量人员配置，投影与节点状态仍只检查 required 下限，可能错误显示“已完成”。F-007 退回 `implementing`，只统一存量投影及状态派生规则。
- 2026-08-10（存量投影修复完成）：保存校验与存量投影已共用同一人数规则，节点状态只消费人员 `affected` 事实；optional 0/1/2、required 0、超过上限及既有候选失效回归通过。完整 `./test/run-f007.sh` 通过，本机未配置 `PLAN_DB_*`，MySQL 集成按约定跳过；状态重新进入 `ready_for_manual`。
- 2026-08-10（第三次人工验收退回）：用户确认表单必须使用原 FormMaking 引擎而不是 Naive UI 字段模拟，且表单数据不能放在节点侧栏；同时发现节点无 hover/click、状态标签重叠、保存成功后前端误报失败，以及配置行存在即让整条路径显示已配置。只读诊断分别定位到缺失独立表单运行时、Vue Flow 节点包装层 `pointer-events:none`、响应式代理 `structuredClone` 和路径级整份保存模型。F-007 重新规划并进入 `awaiting_approval`。
- 2026-08-10（第四次实施启动）：用户批准独立 FormMaking 工作区、逐节点保存、权威状态和基础智能生成闭环。F-007 进入 `implementing`；本轮只实现模板/样本可证明的生成与人工修改保存，不扩张到高级 AI、跨模板学习或目标平台写入。
- 2026-08-10（运行时来源覆盖）：用户明确要求直接采用 `rsh-flow-components` 固定副本，不接受公开 upstream 近似版本或仅迁移最小 FormMaking；同时批准 iframe 会话接收当前验证账号缓存 SID，用于目标组件必要只读请求。已修正文档并保留流程/业务写阻断。
- 2026-08-10（维护流水线补回）：用户指出一次性复制脚本不足以维护运行时，要求迁移旧 V2 ADR-0016 的完整七阶段流水线。当前实现以 pnpm 版本目录替代 Docker 镜像，保留固定来源、来源复核、单活动任务、租约/fencing、在线日志、候选构建、失败回退、健康核验和 Worker 恢复。
- 2026-08-10（第四次实施进行中）：已完成表单生成与独立保存、真实 FormMaking iframe 工作区、逐节点配置与保存对账，以及 `rsh-flow-components` upstream/本地适配分层和维护 API/Worker/页面；状态保持 `implementing`，等待完整自动门禁后再进入人工验收。
- 2026-08-10（第四次实施完成）：完整 `./test/run-f007.sh` 通过，临时 MySQL 集成实际执行；真实表单、智能生成、逐节点/表单独立保存、权威状态、SID 会话隔离及受控维护流水线均形成自动证据。状态进入 `ready_for_manual`，不开始后续运行功能。
- 2026-08-10（第四次人工复核退回）：确认现有同步只更新未被真实入口消费的 upstream 快照，实际入口仍使用独立 vendor FormMaking 且自定义组件注册为空；来源 HEAD 被历史提交永久锁死，维护任务也未真实更新 19001 开发服务或以 HTTP 证明生产 `/form-runtime/` 健康。F-007 退回 `implementing`，按本节完成标准恢复 `rsh-flow-components` 原生运行与同步链路。
- 2026-08-10（原生运行与同步修复完成）：完整 `rsh-flow-components`、真实 FormMaking、20 个自定义组件和原生同步资产已成为唯一 dev/build 输入；维护任务改为创建时记录动态 HEAD、隔离同步构建、同时切换实际源码与生产产物，并以真实 HTTP HEAD/摘要确认或回退。`./test/run-f007.sh` 全部通过，隔离临时 MySQL 未跳过；状态恢复 `ready_for_manual`，等待人工复验，不开始后续运行功能。
- 2026-08-10（运行时写边界独立复核退回）：发现请求策略只覆盖少量流程写路径，完整 `rsh-flow-components` 自定义组件中的上传、文件关联和业务保存接口可能经目标网关放行。F-007 退回 `implementing`，本轮只补齐按方法/语义分类的只读允许与未知写拒绝，不改变目标只读查询、SID 会话或表单协议。
