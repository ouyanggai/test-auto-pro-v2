# 当前进度

- 当前功能：F-007 已保存路径的节点可视化配置
- 当前状态：implementing
- 已完成：已确认返工边界为当前表单配置工作流，保留已通过验收的 F-005 路径交互、数据库结构、目标写接口和 FormMaking 维护链路。
- 阻塞：无；正在实现条件字段真实组件禁用、实时统计与可证明候选切换。
- 下一步：完成当前范围自动验证后回到 `ready_for_manual`，由用户人工核对分支提示、字段禁用、统计和“换一组”；不开始后续运行功能。
- 2026-08-17（第十五次人工验收退回）：当前路径命中条件未在真实 FormMaking 组件层施加字段禁用，提示没有高亮；自动填充统计未按真实值、权限和用户修改重算，“换一组”可能没有实际改变数据。F-007 回到 `implementing`，仅修复本表单工作区边界。
- 2026-08-12（第十四次人工返工完成）：前后端公开协议和 UI 已移除动作次数与内部兼容分组，发起仅提交，审批/协同由独立加签节点列表和唯一处理结果组成；歧义旧记录标记 `affected`。`./test/run-f007.sh`、实际 MySQL、Go build、主前端类型检查与双前端生产构建全部通过，F-007 回到 `ready_for_manual`。
- 2026-08-12（第十四次人工验收退回）：用户要求按 V1 已验证状态推进经验纠正动作交互。F-007 不再显示或配置次数、到达和步骤；审批/协同改为独立加签节点列表与唯一处理结果，状态退回 `implementing`。
- 2026-08-12（第十三次人工返工完成）：动作工具已改用 `@vicons/ionicons5`；提交和默认同意由目录、保存编码、存量投影与前端草稿共同固定一次，替代终止结果不能与同意并存；加签改为新增加签节点的独立处理人策略，第二及后续审批不再依赖当前节点主处理人范围。`./test/run-f007.sh`、实际 MySQL、Go build、主前端类型检查与双前端生产构建全部通过，F-007 回到 `ready_for_manual`。
- 2026-08-12（第十三次人工验收退回）：用户要求正式图标、同意默认且固定一次，并按目标新增审批节点语义修正加签，F-007 退回 `implementing`。
- 2026-08-12（第十二次人工返工完成）：动作说明与禁用原因集中到信息弹层，提交由目录、保存校验、存量投影和前端即时状态共同固定为 1 次；完整动作名称、主题交互和新增区域间距已有结构证据。`./test/run-f007.sh`、实际 MySQL、Go build、主前端类型检查与双前端生产构建全部通过，F-007 回到 `ready_for_manual`。
- 2026-08-12（第十二次人工验收退回）：用户要求动作说明与禁用原因集中收进明显信息按钮，提交固定一次，完整动作中文名称不得截断，动作列表和新增区域需清楚分隔。F-007 退回 `implementing`。
- 2026-08-12（第十一次人工验收退回）：用户要求动作面板只保留动作列表及每行动作次数，删除到达、步骤、前置动作和处理意见；同时第二审批必须能回退到直接前驱。F-007 退回 `implementing`。
- 2026-08-12（第十一次人工返工完成）：动作面板改为平铺动作行与每行次数，完整目录显示禁用原因，新保存不再写处理意见；Java 规则驱动第一审批禁用回退、第二审批自动绑定第一审批，复杂分支不能静态确定唯一直接上一审批时禁用。定向测试和完整门禁通过，F-007 回到 `ready_for_manual`。
- 2026-08-12（第十次独立复核修复完成）：移交改按目标 `checkboxPersonGroup` 多选语义投影，允许目标默认、手动、随机和全选，空选/伪造候选及移交后追加动作均被拒绝。`./test/run-f007.sh`、实际 MySQL、Go build、双前端构建通过，F-007 回到 `ready_for_manual`。
- 2026-08-12（第十次独立复核退回）：目标 `handleHandOver` 接收 `checkboxPersonGroup` 数组并提交完整 `userIds`，当前移交严格单选与真实规则不符。F-007 退回 `implementing`，只修当前动作人数边界。
- 2026-08-12（第九次人工返工完成）：实测正确网关 CORS 已允许 19001、`content-type,sid` 和凭证；旧目标绝对地址改写、权限刷新兼容、动作次数及多动作规则完成。`./test/run-f007.sh`、实际 MySQL、Go build、双前端构建全部通过，F-007 回到 `ready_for_manual`。
- 2026-08-12（第九次人工验收退回）：用户现场发现目标只读请求 CORS、`disabledElement` 异常和动作区无法添加多动作；F-007 退回 `implementing`，只修当前三组边界。
- 2026-08-11（第八次人工返工完成）：节点动作草稿已在面板、父子传递和保存载荷边界逐字段去除 Vue Proxy；真实 reactive 回归捕获到节点 PUT，且载荷修改不回写源草稿。完整 `./test/run-f007.sh` 与实际 MySQL 集成通过，状态恢复 `ready_for_manual`。
- 2026-08-11（第八次人工验收退回）：保存当前节点前对 Vue Proxy 动作数组执行 `structuredClone`，浏览器抛出 `DataCloneError` 且请求未发出。F-007 退回 `implementing`，只修节点保存链路。
- 2026-08-11（第七次独立复核修复完成）：真实 `generalInfo` JSON、`generalInfo__condition` 名称字符串和 `generalInfo__formPersonId` 标识字符串已完整保存并重载；未知组件仅拒绝表单保存，节点人员/动作独立保存不受影响。完整 `./test/run-f007.sh` 与实际 MySQL 集成通过，状态恢复 `ready_for_manual`。
- 2026-08-11（第七次独立复核退回）：现有测试使用全局 `__formPersonId` 和错误的 `__condition` 值形态，未证明真实 `CustomeInfoSelect` 虚拟字段往返。F-007 退回 `implementing`，只修当前证据与可能暴露的最小持久化问题。
- 2026-08-11（第七次人工返工完成）：节点字段/缺口不再进入节点状态、进度或保存；表单运行时按真实 `el` 和实际注册表识别组件，生成器跳过复杂字段但不阻断整表，完整自定义组件值与虚拟字段可保存恢复。`./test/run-f007.sh`、实际 MySQL、Go build、主前端与 form-runtime 双构建全部通过，状态回到 `ready_for_manual`。
- 2026-08-11（第七次人工验收退回）：节点配置仍生成并显示表单字段缺口，智能生成器能力又被误作整张表单兼容性判断；目标模板 `el=custome-info-select` 在适配层丢失并被误判 unsupported。用户要求节点只管人员动作、表单字段全部由独立 FormMaking 服务负责，状态退回 `implementing`。
- 2026-08-11（第六次独立复核修复完成）：人员端点改读真实 `data.dataList`，部门/公司候选仅从缓存的 `flag=3` 子树取得；Go/JavaScript seed 统一限制为安全整数，公开模糊名称删除，移交成为到达终止动作。`./test/run-f007.sh`、实际 MySQL、Go build、主前端与 form-runtime 双构建全部通过，F-007 回到 `ready_for_manual`。
- 2026-08-11（第六次独立复核退回）：真实目标代码证明人员列表应读取 `dataList`，部门/公司候选应从 `flag=3` 树截取；同时发现 63 位 seed 会在 JavaScript 失真、公开模糊名称仍残留、移交未作为终止动作。状态退回 `implementing`。
- 2026-08-11（第六次实施完成）：目标只读目录解析、人员策略、加签/移交复用、有序动作、并行回退可达性、10/100 上限和旧动作迁移均已实现；`./test/run-f007.sh`、隔离临时 MySQL、Go build、主前端与 form-runtime 生产构建全部通过。F-007 进入 `ready_for_manual`。
- 2026-08-11（第六次实施启动）：用户已批准 `040071b` 规划范围，F-007 进入 `implementing`；本轮只补齐目标只读名称解析、受约束人员策略、有序动作与有界重复到达，继续复用既有配置表和表单工作区。
- 2026-08-11（第六次范围重明确）：用户要求所有可读取的人员、岗位、岗级、角色、部门和公司显示具体名称，不能用模糊的运行时说明代替；同时确认节点配置扩展到提交、同意、不同意、暂存、回退、加签、移交和有限重复到达，实例管理动作继续排除。F-007 进入 `awaiting_approval`，用户已授权规划后立即实施。
- 2026-08-07（复核返工）：独立复核发现首次无配置仍显示已保存状态、FormMaking 只解析顶层组件、日期时间允许任意文本；本轮状态为 `implementing`。
- 2026-08-07（人工入口返工）：用户确认路径配置不能隐藏在页面全屏内；增加普通详情路径准备区域、状态摘要、逐条配置/配置下一条、文字编辑路径入口，并在配置保存成功后提供返回详情和配置下一条。
- 2026-08-07（线路入口连续性返工）：用户确认普通流程图右上角需并列显示文字“编辑路径”和“页面全屏”；页面全屏仅查看，编辑路径才进入线路管理。F-007 保持 `implementing`，待定向验证后回到 `ready_for_manual`。
- 2026-08-07（返工验证完成）：首次保存、嵌套 FormMaking、日期控件/严格校验、普通路径准备入口、配置下一条、保存成功返回与普通画布线路入口职责区分均已定向验证；`./test/run-f007.sh` 通过，MySQL 因未配置 `PLAN_DB_*` 跳过。状态回到 `ready_for_manual`，等待人工验收。
- 2026-08-10（计划详情分屏人工返工）：用户反馈路径准备区域大面积绿色且首屏露出半截流程画布；本轮只修中性主题、两个内容视口分屏、CSS scroll-snap、路径列表内部滚动和流程画布剩余高度，状态退回 `implementing`。
- 2026-08-10（计划详情分屏返工完成）：中性主题双屏布局与减少动态效果兼容已实现，`./test/run-f007.sh` 通过；本机未配置 `PLAN_DB_*`，MySQL 集成按约定跳过。状态回到 `ready_for_manual`，等待人工复验。
- 2026-08-10（节点配置方向纠正）：用户确认路径生成和线路编辑没有问题，问题是节点配置脱离流程上下文。原独立表单页面人工验收不通过；重新规划为“无路径先配置路径、已有路径逐条进入独立节点配置画布”，F-007 回到 `awaiting_approval`。
- 2026-08-10（节点画布实施启动）：用户明确批准重新实施，F-007 进入 `implementing`；F-005 路径生成与编辑保持原样，本轮只改已保存路径的节点配置投影、画布和固定面板。
- 2026-08-10（节点画布实施完成）：按真实节点投影配置状态、模板要求和人员候选，配置页已替换为同一 Vue Flow 画布与固定节点面板；`./test/run-f007.sh` 全部通过，本机未配置 `PLAN_DB_*`，MySQL 集成按约定跳过。状态进入 `ready_for_manual`。
- 2026-08-10（最低人数审查返工）：独立复核发现可跳过的人员选择只在必选时校验 `minCount`，导致至少两人的可选会签错误放行一人；F-007 退回 `implementing`，只修当前校验边界。
- 2026-08-10（最低人数返工完成）：前后端已统一为可跳过时零人合法、非零必须达到模板最低人数；optional 0/1/2 人与 required 回归、完整 `./test/run-f007.sh` 均通过，本机未配置 `PLAN_DB_*`，MySQL 集成按约定跳过。状态回到 `ready_for_manual`。
- 2026-08-10（存量配置审查返工）：第二轮独立复核发现旧配置或模板人数变化后的无效人员选择仍可能投影为已完成；F-007 退回 `implementing`，只修存量投影及状态派生一致性。
- 2026-08-10（存量配置返工完成）：保存与投影共用人数规则，旧配置不足最低人数、超过上限或必选为空时均驱动节点“配置失效”；完整 `./test/run-f007.sh` 通过，本机未配置 `PLAN_DB_*`，MySQL 集成按约定跳过。状态回到 `ready_for_manual`。
- 2026-08-10（第三次人工验收退回）：当前实现没有真实 FormMaking 运行时，节点点击被 Vue Flow 包装层禁用，保存成功后因克隆响应式代理误报失败，并且配置行存在即把整条路径判为完成。已完成证据化诊断和返工规划，F-007 回到 `awaiting_approval`。
- 2026-08-10（第四次实施启动）：用户明确批准独立 FormMaking 工作区、基础智能生成、逐节点保存与服务端权威状态；F-007 进入 `implementing`，不开始后续运行功能。
- 2026-08-10（运行时来源覆盖）：用户要求直接以 `rsh-flow-components` 副本作为独立渲染服务，并允许运行时在单次会话内使用已验证账号 SID 完成必要只读请求；原“仅迁移最小 FormMaking 且不传 SID”边界作废。
- 2026-08-10（维护流水线补回）：按旧 V2 ADR-0016 保留固定来源、快照复核、单活动任务、租约/fencing、在线日志、候选构建、不破坏 current、失败恢复 previous 与 Worker 恢复语义；部署单元由 Docker 镜像适配为 pnpm 版本目录。
- 2026-08-10（第四次实施进行中）：真实 FormMaking 工作区、基础智能生成、逐节点/表单独立保存和权威状态已分别提交；运行时固定副本、受控同步维护 API/Worker/页面及定向测试已提交，等待完整 F-007 门禁。
- 2026-08-10（第四次实施完成）：`./test/run-f007.sh` 全部通过；本机 `PLAN_DB_*` 已从忽略配置加载，临时 MySQL 配置/维护迁移与事务测试实际执行。F-007 进入 `ready_for_manual`，等待人工核对真实 FormMaking、SID 会话、自定义组件 unsupported、智能生成、逐节点状态与维护回退。
- 2026-08-10（第四次人工复核退回）：确认当前 upstream 只是闲置快照，实际入口仍走独立 vendor 与空组件注册，固定历史 HEAD 也阻断后续安全快进；维护 RESTART/VERIFY 未作用于 19001 或真实 `/form-runtime/` HTTP 服务。状态退回 `implementing`，只修复原生运行、同步与真实服务更新链路。
- 2026-08-10（原生运行与同步返工完成）：完整 `runtime-source/` 已成为真实 Vue CLI 入口，原生 FormMaking、20 个目标组件、同步资产与 SID 适配同链运行；动态任务 HEAD、隔离候选、真实 HTTP 健康和源码/产物 previous 回退均有定向证据。`./test/run-f007.sh` 全部通过，临时 MySQL 实际执行；状态回到 `ready_for_manual`。
- 2026-08-10（运行时写边界独立复核退回）：完整目标组件源码暴露了现有少量写路径正则的缺口，上传、文件关联和业务保存请求可能绕过策略；F-007 退回 `implementing`，只修复目标请求的只读允许/未知写拒绝边界，完成后再回到 `ready_for_manual`。
- 2026-08-10（运行时写边界返工完成）：XHR/fetch 已统一阻断已知流程/业务/上传/文件关联和未知写语义，已证明只读 POST 仍附加 SID，销毁后恢复原生网络对象；定向测试与 `./test/run-f007.sh` 全部通过，构建产物清理，状态回到 `ready_for_manual`，等待人工复验。
- 2026-08-11（第五次人工验收退回）：节点点击事件链仍被 Vue Flow 选择边界阻断，当前节点保存后错误进入下一路径语义，表单 iframe 可用面积过小；状态退回 `implementing`，只修同路径节点交互/推进与宽屏表单工作区。
- 2026-08-11（第五次人工返工完成）：节点包装层通过官方点击与键盘选择链切换，可配置边界逐节点收紧；保存和 GET 对账统一推进当前路径下一节点，无下一节点时转表单或完成态；真实表单工作区改为单层全宽高主体。`./test/run-f007.sh`、实际 MySQL 集成和双前端构建通过，状态回到 `ready_for_manual`。
- 2026-08-11（表单尺寸复核退回）：主任务发现 form 模式类没有真实 CSS 规则，外层页头、导航和 app-main 留白仍把 iframe 压缩到约 450px，结构检查未验证实际尺寸。状态退回 `implementing`，本轮只修表单可计算全高、全宽与单滚动边界。
- 2026-08-11（表单尺寸复核完成）：form 模式通过 `100dvh - 64px` 和 app-main 留白抵消公式占满主内容区，重复页头/导航隐藏，stage 去卡片，54px 工具栏下 iframe 在 768px 高度为 650px；结构测试改为解析并计算真实规则。`./test/run-f007.sh`、实际 MySQL 和双构建通过，状态回到 `ready_for_manual`。
- 2026-08-11（人员与表单权限人工退回）：节点侧栏未完整展示目标人员规则名称；新发起表单把已安全隔离的提交钩子误判为阻塞且宿主/iframe 告警重叠。F-007 退回 `implementing`，仅修当前投影、权限、钩子隔离和提示边界。
- 2026-08-11（人员与表单权限返工完成）：节点侧栏已结构化展示真实人员规则并提供长列表弹窗；新发起入口权限、已发待发只读、提交钩子隔离、单一提示及 V2-only 保存对账均通过完整自动门禁。状态回到 `ready_for_manual`。
