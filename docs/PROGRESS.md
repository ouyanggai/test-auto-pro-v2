# 当前进度

- 当前功能：F-004 只读真实流程图
- 当前状态：implementing
- 阻塞：新发起模板核对误将保存的模板 ID 放进 `data.id`；目标服务只以顶层 `ids` 过滤，模板超过第一页时会误报目标不可读取。
- 下一步：改用顶层 `ids` 精确筛选、收紧假目标协议测试并重新运行 `test/run-f004.sh`；验证通过后回到 `ready_for_manual`，F-005 尚未开始。
