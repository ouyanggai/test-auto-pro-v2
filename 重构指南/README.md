# test-auto-pro 重构参考包

这四份文档是从 `test-auto-pro` 仓库（分支 `codex/v2-execution-platform`，2868 次提交的完整历史）里实际抽取和分析出来的，不是泛泛的通用建议。建议按顺序读：

1. **`00-诊断报告.md`** — 用 git 提交数据、任务文件状态标记等实证，说明这次 v2 重构具体是怎么跑偏的（节奏失控、UI 框架换了三次、任务分三波越滚越大但验收标准没跟上、规格中途变了但没同步核对）。
2. **`01-PRD-产品需求真相文档.md`** — 唯一权威的产品需求文档，从仓库里最新、最详细的 `CONTEXT.md`（2026-07-21 最后更新）整理而来，附上背景说明。新项目请只信这一份，不要再参考 `v2/CONTEXT.md`（已被取代的旧版本）。
3. **`02-资产清单.md`** — 哪些代码/文档值得带到新项目，哪些不建议直接复用，以及**一个重要提醒：目标平台的真实参考代码 `参考代码/` 没有进 GitHub 仓库（被 `.gitignore` 排除），只在你本地机器上，新项目启动前一定要先把它接上**。
4. **`03-新项目工作方式建议.md`** — 针对诊断报告里三个具体病因给出的对应工作方式调整，不是通用的敏捷/瀑布道理。
5. **`04-架构文档.md`** — 前后端具体技术选型和目录结构落地方案：前端回归纯 Vue3+Vite+Naive UI（不用 Vben 之类的中后台框架），流程画布延用 Vue Flow，后端收回成单体 Go 服务（不拆分成 v2 那样的 7 个独立部署单元），附系统总览图、演进触发条件表和落地顺序建议。

## 关于 GitHub 仓库本身的一个提醒

除了 `参考代码/` 目录缺失，还发现：
- 根目录的 `app-session-O7kcZj7R.js`、`main-BwqrdVu3.js` 这两个文件看起来是构建产物或调试产物，被意外提交进了仓库，新项目起步时建议清理掉。
- `.agents/skills/ask-matt/`——这应该就是你说的"Matt 的技能"，工具本身被完整提交在仓库里了，可以直接确认它当时是怎么配置、怎么被调用的（`SKILL.md` 里应该有完整说明），对复盘"任务是怎么被生成的"很有用。

这些不影响本次重构决策，但顺手提一下，方便新项目起步时环境更干净。
