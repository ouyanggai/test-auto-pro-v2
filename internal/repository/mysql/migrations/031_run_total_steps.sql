-- 2026-09-06 交付验收：运行记录层级与准确进度（编号从 031 起，不复用）。
-- 纪律：path_runs 是事实聚合表，本迁移只加列不改行；既有运行行为不变。
-- total_steps 是本次运行冻结的总步骤数（来自启动时已保存的编译场景长度）：
-- 进度分母必须在启动时冻结，配置后续变化不得改变已运行进度的分母；
-- NULL 表示历史运行（迁移前）没有冻结值，进度分母退化为按 0 处理，界面如实显示「—」。
ALTER TABLE path_runs
  ADD COLUMN total_steps INT NULL;
