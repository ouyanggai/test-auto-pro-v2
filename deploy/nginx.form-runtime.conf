# 表单运行时：直接服务 live 卷上的静态构建（F-023）。主应用以 iframe 固定地址接入。
server {
  listen 19001;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
